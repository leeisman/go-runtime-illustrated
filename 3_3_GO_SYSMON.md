# Book 3.3: 糾察隊長 (Sysmon)

在 **Book 3.2** 中，我們看到 M0 可以透過 `Gosched` 或 I/O 等待來切換 G。這看起來很完美，是一種 **「協作式 (Cooperative)」** 的優雅共舞。

但這世界總是有「奧客」。
如果 G1 寫了一個死迴圈 `for {}`，完全不呼叫任何函數，也不讓出 CPU，怎麼辦？
**後果**: P0 會被 G1 永遠霸佔，其他的 G (包括負責處理網路的) 都會餓死。

這時候，我們需要一位 **「不講武德」** 的執法者 —— **sysmon (System Monitor)**。

---

## 1. 第一樂章：特權階級 (The Privileged M)

在 Go Runtime 啟動時，除了 M0 之外，還會偷偷啟動一條非常特殊的執行緒：**sysmon**。

### 1. 他的特權
所有的普通 M (M1, M2...) 想要工作，都必須先搶到一個 **P** (辦公桌)。沒有 P，M 就只能睡覺。
但 **sysmon 不需要 P**。
*   **身分**: 他直接隸屬於系統，不歸任何 P 管轄。
*   **作息**: 他不參與普通的調度循環。他有自己的生理時鐘 (休眠 -> 喚醒 -> 巡邏 -> 休眠)。
*   **隱喻**: M0, M1 是 **銀行櫃員** (必須坐在櫃台 P 才能辦事)。sysmon 是 **保全隊長** (一直在大廳走來走去，不用坐櫃台)。

### 2. 巡邏節奏 (The Loop)
Sysmon 非常勤勞，但他也很懂得節能。
*   **剛啟動時**: 每 20 **微秒** (us) 巡邏一次 (非常頻繁)。
*   **沒事發生時**: 慢慢延長睡眠時間，最長每 10 **毫秒** (ms) 醒來一次。
*   **任務**: 每次醒來，他就像拿著手電筒的巡夜人，檢查圖書館的每一個角落。

---

## 2. 第二樂章：三大執法任務 (The Three Duties)

Sysmon 的存在就是為了處理那些 **「櫃員 (M) 無法處理」** 或 **「櫃員正在偷懶」** 的狀況。

### 任務一：強制搶佔 (Preemption) —— 對付惡霸 G
這是 Sysmon 最重要的職責，解決了 Go 1.14 之前最令人頭痛的「死迴圈卡死」問題。

1.  **巡邏**: Sysmon 走過 P0 櫃台，看了一眼 M0 正在執行的 G1。
    *   他會記錄：「現在時間 12:00:00，P0 正在跑 G1」。
2.  **再巡邏**:過了 10ms，Sysmon 又走回來。
    *   他發現：「現在時間 12:00:10，**P0 怎麼還在跑同一個 G1？**」
3.  **判定**: G1 佔用 CPU 超過 10ms，判定為 **「超時違規」**。
4.  **執法 (ASync Preemption)**:
    *   Sysmon 不會客氣，他直接向 M0 發送一個 **OS 信號 (SIGURG)**。
    *   **M0 的反應**: M0 (Thread) 收到信號，被迫暫停手邊的快樂死迴圈，轉而去執行 **信號處理函數 (`doSigPreempt`)**。
    *   **結果**: 這個處理函數會在 G1 的 Stack 上強行插入一行指令：「**不用跑了，去睡覺 (`gopreempt_m`)**」。
    *   M0 隨即立刻執行 `schedule()`，G1 被踢回 Global Queue，換 G2 上場。

### 任務二：沒收資源 (Retake P) —— 對付卡死的 M
有時候不是 G 壞，是 M 自己卡住了。

1.  **情境**: M1 呼叫了一個 C 語言的 Library (CGO) 或者執行了某些長時間的 System Call。
2.  **後果**: M1 進入了 **System Mode**，整個人僵住不動。
3.  **Sysmon 的視角**:
    *   「喂，M1，你手上抓著 P1 (辦公桌)，結果你去跟 OS 聊天聊了這麼久？」
    *   P1 的 Local Queue 裡還有其他 G 在排隊耶！
4.  **執法**:
    *   Sysmon **直接把 P1 從 M1 身上拔下來**。
    *   Sysmon 把 P1 交給另一個空閒的 M (或叫醒一個新的) 去接手工作。
    *   **M1 的下場**: 等 M1 終於聊完天回來，發現手上的 P1 不見了。他只好乖乖把手上的 G 丟回 Global Queue，然後自己去睡覺 (Thread Park)。

### 任務三：協助收信 (Netpoll) —— 避免遺漏
雖然 M 在 `schedule()` 時會順便檢查網路，但如果所有 M 都在忙著算數學 (CPU Bound)，可能好幾秒都沒人即使去 Netpoller 檢查。

1.  **Sysmon 的動作**:
    *   每次巡邏，只要發現有空閒時間，就會去 **Netpoller 櫃台** 瞄一眼：**`epoll_wait`**。
2.  **發現**: 「喔！Socket 5 和 Socket 6 有資料了！」
3.  **處置**:
    *   Sysmon 把對應的 G5, G6 領回來。
    *   直接丟進 **Global Queue**，確保它們能盡快被任何一個 P 撿去執行。

---

## 3. 第三樂章：隱喻總結 (The Metaphor)

如果說 **GMP** 是這間圖書館的 **運作制度**，那 **Sysmon** 就是 **風紀組長**。

*   **他是獨立的**: 不受制於任何 P，直接聽命於 OS (Runtime Root)。
*   **他是無情的**:
    *   你跑太久？**踢！** (Preemption)
    *   你卡太久？**沒收裝備！** (Retake P)
    *   你有信沒收？**我幫你收！** (Netpoll Injection)

正因為有這位 **24 小時不休息、每 10ms 巡邏一次** 的糾察隊長，Go 的協作式調度才不會因為某個 G 的惡意行為而全盤崩潰。

接下來的 **Book 3.4**，我們將深入探討 Sysmon 幫忙巡邏的那個神秘櫃台 —— **Netpoller**。

# Book 3.4: 網路掛號處 (The Netpoller)

在 Book 3.2 中，我們看到 M0 如何在純 CPU 運算 (G1, G2) 之間快速切換。
但如果是 **I/O 任務** 呢？例如 `http.Get("google.com")`，這可能要等 500ms，對 CPU 來說是永恆。

如果 M0 陪著 G1 乾等，那這 500ms 內整個 P0 就停擺了。
為了解決這個問題，Go 在 303 號房的角落，設了一個特殊的設施：**Netpoller (網路掛號處)**。

---

## 1. 第一樂章：阻塞的難題 (The Blocking Problem)

**情境**:
*   M0 正在執行 G1。
*   G1 執行了一行代碼：`net.Read(socket)` (例如讀取 Google 的回傳)。

**兩條路 (The Fork)**:
1.  **傳統路徑 (Blocking Syscall)**:
    *   如果 M0 真的去呼叫 OS 的 `read` 系统呼叫。
    *   **OS 的制裁**: **行政區 (OS)** 發現沒資料，直接將 **M0 (Thread)** 的狀態改為 **WAITING**，並把它踢出 CPU。
    *   **M0 的處境**: M0 就像被施了定身法，僵在「紅色服務鈴」前動彈不得，等待網卡通知。
    *   **P0 的災難**: M0 是 P0 唯一的館長。M0 睡著了，意味著 **P0 清單上排隊的 G2, G3, G4... 全部被迫停工**。
        *   *(隱喻: 就像銀行只有一個櫃台，櫃員為了幫第一位客人查資料卡住了，後面排隊的 100 個人雖然都是簡單的存錢，卻也只能乾等。)*
    *   **代價**: 為了維持服務，你必須為每位客人都開一個櫃台 (Thread per Request)，這就是 **Book 1** 提到的資源耗盡噩夢 (C10K)。

2.  **Go 的魔法路徑 (Non-blocking via Netpoller)**:
    *   Go 的目標是：**絕不讓 M0 睡著**。即使 G1 需要等待，M0 也必須保持清醒去服務別人。
    *   因此，Go 的 `net` 套件在底層把這個動作偷換成了 **「掛號 (Epoll)」**。

---

## 2. 第二樂章：掛號機制 (The Registration)

Go 的 Runtime 為了不讓 M0 睡著，採取了完全不同的策略。

**動作流程**:
1.  **發現阻塞**: 當 G1 呼叫 `net.Read` 時，Go 的 `net` 套件會發現 socket 還沒準備好。
2.  **掛號 (The Registration - `epoll_ctl`)**:
    *   Go Runtime 不會呼叫阻塞的 `read`，而是改走 **「快速通道」**。
    *   **前往行政區**: M0 帶著一張 **「監控申請單 (`epoll_ctl`)」** 來到行政區的 **「Epoll 快速受理窗口」**。
    *   **遞交申請**: M0 對櫃台說：「請幫我監控 **Socket #5**。如果有資料進來，請投遞到我們 303 號房的 **Netpoller 信箱**。」
    *   **行政區回應**: 櫃台人員蓋章確認：「好，已登錄。**你可以回去了，不用在這裡等。**」
    *   **立即回歸**: 這是關鍵！M0 **沒有被扣留**。他辦完手續只花了幾百奈秒，立刻就走回了 303 號房。
3.  **封存 G1**:
    *   M0 把 G1 的狀態 (PC, SP) 存好，將其標記為 **`Gwaiting`**。
    *   **關鍵**: M0 把 G1 **丟在這個 Netpoller 櫃台**，而不是放回 P0 的 Queue。
4.  **M0 脫身**:
    *   現在 M0 手空了，而且他也沒睡著。
    *   M0 轉身回到 P0，拿起下一個 **G2** 繼續跑。
    *   *(回顧 Book 3.2: 這裡 M0 不需要透過 OS，直接原地轉頭 (Change SP) 執行 G2，依然保持 User Space 的極速。)*
    *   **成果**: P0 的生產線完全沒停，儘管 G1 在等網路。

---

## 3. 第三樂章：喚醒與回歸 (The Wakeup)

過了 500ms，Google 的回應終於透過網路線傳到了網卡。

**動作流程**:
1.  **硬體中斷**: 網卡通知 OS：「資料到了！」
    *   *(回顧 Book 1.3: 這是硬體層的中斷訊號，迫使 CPU 暫停手邊工作來處理網卡。)*
2.  **Epoll 就緒**: OS 查了一下表，發現 Go 之前有掛號這個 socket。於是 OS 把這個事件記錄在 Epoll 的就緒清單裡。
3.  **Sysmon 巡邏 (或 Schedule)**:
    *   Go 有一個專門的巡邏隊 (Sysmon) 或者 M0 自己在 `schedule()` 時，會順便去 Netpoller 櫃台看一眼：**`epoll_wait`**。
    *   OS 說：「有！Socket 5 有資料了！」
4.  **領回 G1**:
    *   Runtime 發現 Socket 5 對應的是 **G1**。
    *   Runtime 把 G1 從櫃台領回來，將狀態改回 **`Grunnable`**。
    *   **歸隊**: G1 被放回 **Global Queue** (或者注入某個 P 的 Local Queue)。
5.  **執行**:
    *   下次 M0 (或 M1) 去 Global Queue 找工作時，就會撿起 G1。
    *   G1 醒來，以為自己只睡了一覺，繼續執行 `net.Read` 後面的代碼。

---

## 4. 第四樂章：Event Driven 的黑盒子 (Inside Epoll)

你可能會好奇，行政區 (OS) 是怎麼知道 Socket 5 有資料的？難道它一直盯著所有的 Socket 看嗎 (Polling)？
不，這就是 **Event Driven** 的精髓。

### 1. 登記 (Red-Black Tree)
當 M0 執行 `epoll_ctl` (掛號) 時，行政區會在一個巨大的 **紅黑樹 (RB-Tree)** 上掛上一個節點：
> 「監控目標：Socket 5」
> 「動作：資料來了請執行回調函數 `ep_poll_callback`」。

### 2. 重要機制：回調 (The Callback)
這就像是設了一個 **機關陷阱**。
*   平靜時，紅黑樹靜靜地躺著，消耗極少的資源。
*   當 **網卡中斷 (Interrupt)** 發生，資料衝進 Socket 5 的 Recv Buffer 時，陷阱被觸發了！
*   **連結 Book 1.3**: 還記得 SoftIRQ 負責收信嗎？在 Epoll 模式下，SoftIRQ 在收到信後，會多做一個動作：**檢查這棵紅黑樹**。
*   **觸發**: SoftIRQ 發現 Socket 5 有登記，於是順著機關，自動執行了設定好的回調函數 `ep_poll_callback`。

### 3. 就緒清單 (The Ready List)
*   這個回調函數只做一件事：**把 Socket 5 的名字，抄寫到一張「就緒清單 (Ready List)」上。**
*   這張清單只包含 **真正有資料的 Socket**。即使你有 10,000 個連線，如果只有 1 個有資料，清單上就只有 1 行字。

### 4. 查詢 (Getting Results)
*   當 M0 後來回來問 (`epoll_wait`) 時，行政區 **不需要** 去掃描那 10,000 個連線。
*   行政區只需要把這張 **就緒清單** 拿給 M0 看：「喏，只有這幾個有信。」
*   這就是為什麼 Epoll 這麼快 (O(1))：**它只處理活躍的連線，完全無視靜默的連線。**

---

## 5. 第五樂章：隱喻總結 (The Metaphor)

*   **Netpoller = 餐廳的候位叫號機**。
    *   你 (G1) 去餐廳 (Socket) 吃飯，但沒位子 (沒資料)。
    *   你不用站在門口罰站 (Blocking)。
    *   你領一張號碼牌 (Epoll Register)，然後去旁邊逛街 (Yield)。
    *   有位子了，機器會叫你 (Wakeup)，你再回來吃。
*   **M0 = 盡責的服務生**。
    *   他絕對不會陪客人一起等位子。他發完號碼牌就去服務下一桌客人了。

這就是為什麼 Go 能夠用極少的 M (Threads)，支撐成千上萬個併發網路連線 (C10K Problem) 的秘密。

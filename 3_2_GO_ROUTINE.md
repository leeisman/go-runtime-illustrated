# Book 3.2: 單核魔術 (The Solo Magician)

在 Book 3.1 中，我們見證了 **M0** 的誕生和 **G1** 的啟動。
但 Go 的強大之處在於 **「併發 (Concurrency)」**。如果我們只有一個 M0 (一個 CPU 線程)，它真的能同時做兩件事嗎？

這一章，我們將用顯微鏡觀察：**當 M0 只有一個人時，他是如何在兩個 Goroutine 之間玩弄魔術的。**

---

## 1. 第一樂章：雙人舞 (The Duet)

### 前情提要：主角的身分
在 **Book 3.1** 中，我們見證了 **OS Thread (T0)** 如何透過連結 **g0 控制台**，變身為 Go Runtime 的 **M0**。
现在，M0 正坐在 P0 區域前，準備執行我們交給他的代碼。

### 劇本代碼
讓我們寫一段最簡單的 Go 代碼，只有兩個主角：
1.  **G1 (Main)**: 原本的主角。
2.  **G2 (Helper)**: 新加入的舞伴。

```go
package main

import (
	"fmt"
	"runtime"
)

func main() { // <--- G1
	// 1. 創造 G2
	go func() { // <--- G2
		fmt.Println("G2: 我是新來的!")
	}()

	// 2. G1 讓出舞台 (主動切換)
	fmt.Println("G1: 我先休息一下...")
	runtime.Gosched() // <--- 關鍵指令：請求切換

	// 3. G1 回來
	fmt.Println("G1: 我回來了!")
}
```

---

## 2. 第二樂章：慢動作解析 (Frame by Frame)

### 場景 1: 創造 (Birth of G2)
*   **現狀**: M0 正在執行 G1。P0 的清單是空的。
*   **動作**: G1 執行到 `go func()`。
*   **底層**: M0 呼叫 `newproc()`。
    *   M0 在黑板 (Heap) 上填了一張新的任務單 **G2**。
    *   M0 把 **G2** 放入 **P0 的清單 (Local Queue)** 中。
*   **結果**: 
    *   M0 手上正在跑 G1。
    *   P0 清單裡躺著 G2。

### 場景 2: 讓出 (The Yield - `Gosched`)
*   **動作**: G1 執行到 `runtime.Gosched()`。這是一個很有禮貌的函數，意思是「我的回合還沒結束，但我願意先讓別人跑」。
*   **切換開始 (Switch Start)**:
    *   M0 執行 `mcall(gosched_m)`。
    *   **跳回 g0**: M0 瞬間將 CPU 的 **SP (Stack Pointer)** 切換回 **g0 主閱讀桌**。
    *   **保存 G1**: M0 把 G1 目前讀到的行數 (PC) 和暫存器狀態，寫在 G1 的小本子 (`g.sched`) 裡。
    *   **歸隊**: M0 把 G1 放回 **Global Queue** (或 P0 Queue 的尾端)，讓它重新排隊。

### 場景 3: 調度 (The Schedule)
*   **現狀**: M0 現在坐在 g0 桌前，手上是空的。
*   **尋找**: M0 執行 `schedule()`。
    *   他看了一眼 P0 清單。
    *   **發現 G2！** (因為 G1 剛剛讓出了，G2 是下一個)。
*   **決定**: 「好，下一個跑 G2。」

### 場景 4: 執行 G2 (Running G2)
*   **動作**: M0 執行 `execute(G2)`。
*   **切換 (Context Switch)**:
    *   M0 讀取 G2 小本子裡的 `PC` (第一行代碼) 和 `SP` (Stack地址)。
    *   **跳躍**: M0 將 CPU 的 SP 指向 G2 的 Stack，將 PC 指向 G2 的函數入口。
*   **結果**: 螢幕上印出 `"G2: 我是新來的!"`。

### 場景 5: G2 謝幕 (G2 Exit)
*   **動作**: G2 跑完了，函數返回。
*   **清理 (`goexit`)**:
    *   M0 執行 `goexit`。但他**不會**把 G2 丟給垃圾車 (GC)。
    *   **回收 (Recycle)**: M0 把 G2 的表格擦乾淨，放回 **P0 的備用倉庫 (`gfree` list)**。
    *   **意義**: 這不是 GC，這是 **物件重用 (Pooling)**。下次若有人再呼叫 `go func()`，M0 就可以直接從倉庫拿這個舊表格來用，連記憶體都不用重新申請。這就是 Go 為什麼這麼快的秘密。
*   **回歸**: M0 再次跳回 **g0**。
*   **再調度**: M0 再次執行 `schedule()`。
    *   這次他在 Queue 裡（可能在 Global Queue）找回了剛剛休息的 **G1**。
    *   M0 重新載入 G1 的進度。
*   **結果**: 螢幕印出 `"G1: 我回來了!"`。

---

## 3. 第三樂章：顯微鏡下的物理學 (The Physics)

你可能會問：「M0 跳回 g0，再跳去 G2，這聽起來很慢？」
為了理解這為什麼**快**，我們必須對比一下 **OS Thread** 的切換。

### 1. 傳統 OS Thread 切換：換房間並保存上下文
當 OS 要從 Thread A 切換到 Thread B 時，就像是館長要從 **A 辦公室** 搬到 **B 辦公室**：
*   **清場**: 館長必須把 A 房間的所有文件歸檔。
*   **卸甲**: 他必須脫下 A 號重型機甲，交還給行政區。
*   **移動**: 走出門，經過漫長的行政走廊 (Kernel Space)。
*   **著裝**: 領取 B 號機甲，穿上，再把 B 房間佈置好。
*   **代價**: 消耗 **幾微秒 (us)**。這在 CPU 世界裡相當於好幾年。

### 2. Goroutine 切換：原地轉頭
Go 的切換 (User-Space Switch) 則完全不同。M0 **從頭到尾都沒有離開過這間房間 (User Space)**，他也**沒有脫下身上的 M0 機甲**。
*   他只是在看這張紙 (G1)，時間到了。
*   **轉頭**: M0 保存一下進度，然後 **轉個頭 (Change SP Register)**。
*   **聚焦**: 視線直接對準了另一張紙 (G2)。
*   **代價**: 消耗 **幾十奈秒 (ns)**。就像眨眼一樣快。
*   **因為**: 因為只改了暫存器，沒有驚動 **行政區 (OS)**，也不用過海關 (Syscall)。

這就是為什麼單核的 M0 也能像魔術師一樣，讓 G1 和 G2 看起來像是在同時跳舞。

---

## 4. 第四樂章：Q&A - 記憶體會爆炸嗎？ (Memory Management)

**使用者問**：「如果我一口氣產生了 10,000 個 Goroutine，跑完之後它們都變成了屍體堆在 `gfree` 裡，那記憶體豈不是永遠不會釋放？」

**答案**：Go 沒那麼笨。它有一個 **「先囤貨，後出清」** 的策略。

1.  **Local Cache (P 的背包)**：
    *   每個 P 的 `gfree` 清單其實有容量上限 (例如 64 個)。
    *   M0 覺得背包太重時，會把多出來的舊 G 丟回 **Global Queue** 的倉庫。

2.  **Global Warehouse (全域倉庫)**：
    *   這裡可以囤積較多的舊 G，供所有 P 共享。

3.  **GC 的大掃除 (Scavenging)**：
    *   如果這 10,000 個 G 跑完後，很久都沒有新的任務進來。
    *   當 **系統級垃圾回收 (GC)** 啟動時，它會檢查這個倉庫：「怎麼堆了這麼多廢紙？」
    *   **裁員**: GC 會把這些長期閒置的 G 結構體徹底釋放，將記憶體歸還給 OS。

**結論**：
*   **短期**: 為了快，Go 會保留屍體 (Reuse)。
*   **長期**: 為了省，GC 會清理屍體 (Release)。

### Pro Tip: 關於 Worker Pool 的迷思
既然 Go 內部已經有了 `gfree` 這個超強的物件池，那你還需要自己寫 **Worker Pool** 嗎？

*   **為了效能 (Reusing)**？ -> **不需要**。
    *   不要因為怕 `go func` 太慢而自己寫池。Go Runtime 的 `gfree` 機制已經幫你把創建成本降到最低了。
*   **為了限流 (Throttling)**？ -> **需要**。
    *   如果你只有 100 個資料庫連線，卻一口氣開了 10,000 個 G，DB 會爆掉。
    *   這時候寫 Worker Pool (或 Semaphore) 的目的不是為了省記憶體，而是為了 **控制同時執行的人數**。

### 結語：灑脫的代碼 (The Freedom of Go)

學習了這麼多底層原理 (`gfree`, `GMP`, `Context Switch`)，最終的目的不是讓你寫代碼時變得綁手綁腳。
恰恰相反，這些知識是為了讓你寫得更 **灑脫**。

Go 語言團隊解決了所有沉重的底層難題，就是為了讓你可以毫不猶豫地寫下 `go func()`。
**面對 I/O 任務，請盡情地灑脫。**

只需記住唯一的底線：**CPU 核心數是物理的，資料庫連線是有限的。**
除此之外，享受 M0 為你帶來的自由吧。

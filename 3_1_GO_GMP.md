# Book 3.1: 書中真正的內容 _rt0_go (The True Content)

## 0. 前言：被隱藏的真實開端

在 **Book 2** 中，我們探索了 303 號房的記憶體配置 (Stack/Heap)。但當時我們有一個巨大的誤解 —— 我們以為館長打開書本後，是直接從 `main()` 開始閱讀你寫的故事。

**事實上，`main` 根本不是這本書的第一頁。**

如果你用放大鏡檢視 Go 的執行檔，你會發現真正的第一頁是一段被隱藏的引言 —— **`_rt0_go`**。

這段引言不是故事本身，而是一份 **「高效能閱讀指南」**。
它要求館長在開始讀故事之前，先建立一套強大的 **GMP 調度機制**。這就是為什麼 Go 語言能同時處理成千上萬個任務的秘密。

---


## 1. 第一樂章：起源 (The Origin - `_rt0_go`)

讓我們回到 303 號房剛被打開的那一瞬間。

### 1. 主角登場 (Enter T0)
*   一位穿著 **重型機甲** 的館長走了進來。此時，他只有一個 OS 賦予的編號：**T0 (Thread 0)**。
*   房間裡只有一張 **「主閱讀桌」 (System Stack, 8MB)**。T0 沒得選，只能坐在這張桌子前。

### 2. 閱讀指令 (Execution)
*   T0 坐在這張 **主閱讀桌** 前，翻開書本的第一頁，讀到了 **`_rt0_go`** 章節。
*   書上寫著一連串 **「自我改造」** 的指令，T0 開始照做：

### 3. 改造過程 (The Transformation)

#### A. 建立檔案 (Initializing Global Objects)
1.  **尋找物件**: T0 依指令指示，走到 **大黑板 (Heap/Global Area)** 前。他發現那裡已經有兩個 **由編譯器預先保留** 的全域變數空間：**`runtime.g0`** 和 **`runtime.m0`**。
2.  **定義 g0 (我的辦公桌)**:
    *   T0 拿出尺，測量目前 **OS Stack (8MB)** 的範圍 `{lo, hi}`，並填入 `g0.stack`。
    *   **功能**: g0 代表了這條 Thread 的 **「執行能力 (Stack)」**。
3.  **定義 m0 (我的員工證)**:
    *   **本質**: m0 是這條 Thread 在 Go 世界裡的 **「身分證 (Employee Record)」**。
    *   **內容**: 這張卡片上會隨時記錄 `curg` (執行中 G), `p` (歸屬 P), `g0` (系統棧)。
    *   **意義**: T0 是肉體 (OS Thread)，m0 是他在 Go 公司的 **職位與狀態**。沒有 m0，Go Runtime 就不知道這條 Thread 是誰。
4.  **建立關聯**: T0 將兩者互聯：`m0.g0 = g0`，`g0.m = m0`。

```go
type g struct {
    stack stack   // 辦公桌範圍
    m     *m      // 這張桌子屬於誰 (m0)
}

type m struct {
    g0      *g       // [裝備] 指向那個負責調度的 g0 (System Stack)
    curg    *g       // [任務] 此刻正在執行的 G (可能是 G1)
    p       uintptr  // [歸屬] 綁定的 P (資源與隊列)
    nextp   uintptr  // [預備] 暫存的 P (Handoff 交接時用)
    id      int64    // [編號] 員工代號 (M0, M1...)
    tls     [6]uintptr // [私房錢] Thread Local Storage
}
```

#### B. 設定連結插槽 (Configuring FS Register) - 導航系統開啟
*   **你的疑問**: 「這個連結到底是在連什麼？跟使用 Stack 有關嗎？」
*   **答案**: 關係非常大！這是在設定 CPU 的 **「快速存取鍵 (Shortcut)」**。
*   **動作**:
    1.  T0 呼叫 OS 指令 `arch_prctl`，把 **`g0` 的記憶體地址** 寫入 CPU 的 **FS 暫存器**。
    2.  **連結對象**: **硬體 (Register)** <---> **軟體物件 (g0)**。
*   **為什麼要這樣做？ (Accessing Stack)**:
    *   當程式碼需要「存取我的 Stack」或「存取 m0」時，它不需要去全域大海撈針。
    *   CPU 只要讀取 `FS` 暫存器，就能瞬間找到 `g0`。
    *   找到了 `g0`，也就找到了 `g0.stack` (主閱讀桌的位置)。
    *   **結論**: 這個連結動作，就是讓這條 Thread **「認得自己的辦公桌在哪裡」** 的關鍵步驟。

#### C. 變身完成 (Transformation Complete)
*   **生效**: 隨著系統呼叫返回，T0 感覺到了 FS 插槽的鎖定。
*   **鏈路確認**: 他低頭讀取 FS，順利連線到了 **g0 控制台**。
*   **自我認知**: 他看了一眼控制台上的標籤 (`g0.m`)，上面寫著指向 **`runtime.m0`**。
*   **結論**: 「既然我連上了 g0，而 g0 屬於 m0，那我就是 **M0**。」
*   **現狀 (The Connection)**:
    *   M0 (T0) 準備開始執行下一章節的代碼：**`runtime.mstart`**。
    *   **你的疑問**: 「g0 怎麼對應到這段代碼？」
    *   **解答**: 執行函式 (`mstart`) 需要記憶體來暫存變數和紀錄位置 (Call Stack)。
    *   **對應關係**:
        *   `g0` 這個物件裡記錄了記憶體範圍 `{lo, hi}`。
        *   當 `mstart` 執行時，它產生的所有 **Stack Frames**，都會被寫入 **g0 所描述的那塊記憶體空間裡**。
        *   所以我們常說：「M0 正在 **g0 stack** 上執行代碼」。
    *   **隱喻**: `mstart` 是 **劇本 (Code)**，`g0` 是 **舞台 (Stack)**。M0 現在要在 g0 這個舞台上演出 `mstart` 這場戲。

---

## 2. 第二樂章：創世 (Genesis - Initialization)

現在，**T0** 已經完成了身分轉換。他確認了與 **g0 控制台** 的連線，並接受了 **M0** 這個新代號。從此刻起，為了方便敘述，我們將這位登錄了系統的 T0 稱為 **M0**。

他站在 **g0 主閱讀桌** 前，繼續往下閱讀初始化章節。書上寫著：「在開始營業之前，你需要先建設好圖書館的基礎設施。」
為了執行這些複雜的建置工作，M0 (T0) 必須依賴這張 **g0 主閱讀桌** 來記錄所有的臨時數據。

### 1. 指令一：確認人力 (`runtime.osinit`)
*   **記錄進度 (Stack)**：館長在 **g0 主閱讀桌 (System Stack)** 上寫下：「正在執行 `osinit`」，這樣他才知道自己做到哪一步。
*   **執行並發布 (Heap)**：他打電話問到 NCPU 數量後，將這個數字寫在 **303 號房的大黑板 (Heap)** 的公告欄上。
*   *原因*：人力數量是所有人都需要知道的資訊，所以必須寫在公共區 (Heap)，不能只寫在私人的 **g0 主閱讀桌 (Stack)** 裡。

### 2. 指令二：設立待辦清單 (`runtime.schedinit` -> `procresize`)
*   **點人頭 (Count)**：館長先看了一眼 `GOMAXPROCS` 環境變數。如果沒設定，就預設等於 CPU 核數 (例如 **8**)。
*   **劃分區域 (Allocation)**：館長依照這個數字，在 **303 號房的大黑板 (Heap)** 上，劃出了 **8 個待辦清單區域**。
*   **命名 (Identity)**：我們稱這 8 個區域為 **P0, P1, ... P7 (Processors)**。
*   **佈置 (Init)**：
    *   在每一個區域裡，館長都畫上了一個 **容量為 256 格的待辦欄位 (Local Queue Array)**。這是未來用來填寫輕量任務單 (G) 的地方。
    *   **P0 的特權**：M0此時宣佈 **「接管」 P0 區域** (`_g_.m.p = p0`)。
    *   **區分**: M0 身體還是坐在 **g0 主閱讀桌** 前，但他現在負責管理 P0 欄位裡的所有資料。
        *   **閒置 (Idle)**：至於剩下的 **P1 ~ P7**，目前欄位是空的，也沒有人 (M) 來負責。
*   **位置的重要性 (Global Visibility)**：這 8 個清單被刻意劃在 **Heap (公共區)** 上，而不是藏在 g0 的私有抽屜裡。這意味著這些清單擁有 **全域可見性**，允許未來不同的 CPU 核心 (Threads) 互相存取彼此的待辦事項，是實現高併發調度的基礎。

---

## 3. 第三樂章：初生 (The First G - `newproc`)

終於，基礎設施 (M0, g0, P) 都準備好了。但現在還沒有任何「工作」可做。
你可能會疑惑：「我的程式碼不是有寫 `func main()` 嗎？直接執行不就好了？」

**不行。** 在 Go 的世界裡，**所有的代碼都必須在 Goroutine (G) 裡面才能執行**，連 `main` 函數也不例外。
所以，M0 必須手動創造这個世界的第一個 G。這是唯一一次不需要你寫 `go` 關鍵字，Runtime 自動幫你呼叫 `newproc` 的時刻。

### 1. 創建 G1 (Main Goroutine)
*   M0 在 g0 桌上執行了 **`newproc(runtime.main)`**。
*   他在 Heap 上撕下一張新的 G 表格，此即為 **G1**。
*   **填寫入口**: M0 把 **`runtime.main`** 的地址填入 G1 的 PC 欄位。
*   **連結用戶代碼**: 這個 `runtime.main` 稍後被執行時，就會去呼叫你寫的 **`main.main`**。所以，你的 `main` 函數其實是 G1 的一部分。

### 2. 派發 (Enqueue)
*   M0 把這張嶄新的 **G1** 任務單，輕輕地放進了自己 (P0) 的 **公文籃 (Local Queue)** 裡。
*   現在，P0 的籃子裡有了第一件工作。

---

## 4. 第四樂章：永動 (Perpetual Motion - `mstart`)

一切就緒。M0 做了一個深呼吸。
他要執行最後一個指令：**`mstart` (開始營業)**。

### 1. 變身 (The Switch)
*   M0 離開了 **g0 主閱讀桌**。
*   他走到 P0 的公文籃前，拿起了唯一的任務：**G1**。
*   他開始執行 G1。

### 2. 使用者代碼執行
*   G1 開始跑。它先做了一些 runtime 初始化 (`init` functions)，然後終於呼叫了 **`main.main`**。
*   **這一刻，你寫的第一行代碼 `fmt.Println("Hello World")` 終於被 CPU 執行了。**

---

## 5. 第五樂章：調度策略 (The Strategy - Steal & Handover)

當 M0 執行完 G1 後，並不會停下來。他會繼續執行 `schedule()`，這才是 GMP 模型真正強大的地方。

### 1. 竊取機制 (Work Stealing)
*   **情境**: 假設 P0 的籃子空了。M0 不會去睡覺，而是展現了極高的積極性。
*   **搜尋順序**:
    1.  先看 **全域清單 (Global Queue)** 有沒有沒人要的 G。
    2.  如果沒有，就去 **偷看別人的籃子 (P1, P2...)**。
    3.  如果 P1 有 4 個 G，M0 會直接 **偷走一半 (2 個)** 放到自己的籃子裡做。
*   **隱喻**: 勤奮的店員做完自己的事後，不會滑手機，而是轉頭看隔壁同事桌上有沒有堆積的公文，直接拿一半過來幫忙做。這保證了所有 CPU 核心都不會閒置。

### 2. 交接機制 (Handoff)
*   **情境**: 假設 G1 呼叫了一個 **阻塞式系統呼叫 (Blocking Syscall)** (非網路 I/O，例如 CGO)。
*   **後果**: M0 被卡住了，被迫在原地罰站等待 OS。
*   **P0 的反應**: P0 (籃子) 裡面還有其他 G 在排隊啊！不能陪 M0 一起死。
*   **動作**: P0 會 **「斷開連結」** (Detach) 離開 M0。
*   **尋找新歡**: P0 會去睡眠區叫醒一個備用的 M (比如 M1)，或者創造一個新的 M，然後把自己掛到 M1 身上繼續工作。
*   **隱喻**: 店員 A 被奧客纏住 (Block) 了。經理 (P) 立刻把整籃訂單搬走，交給店員 B 繼續處理。店員 A 就留在原地慢慢應付奧客。

這兩大機制 (Stealing & Handoff) 確保了：
*   **CPU 不閒置** (沒事做就去偷)。
*   **佇列不阻塞** (有人卡住就換人做)。

---

## 6. 第六樂章：Runtime 的真相與細節 (The Runtime Reality)

我們經常把 Runtime 講得很神祕，但說穿了，它就是一套寫好的 **員工守則 (SOP)**。

### 1. M 的本質：披著 Go 皮的 OS Thread
*   **真相**: 對 OS 來說，**M 就是普通的 Native Thread (pthread)**。
*   **變身**: Go 只是在 Thread Local Storage (TLS) 裡存了一個指標指向 `runtime.m`，把它包裝成了 M。
*   **M0 與 Runtime**:
    *   **M0** 只是第一位來報到的員工。
    *   **Runtime** 是「公司制度」。所有 M (M0, M1...) 都在執行 Runtime 的代碼 (如 `schedule()`, GC)。
    *   **M 是執法者**: 不要把「警察 (M)」跟「法律 (Runtime)」混為一談。

### 2. 調度器的視角：誰在記帳？
當切換發生時，有兩本帳在記：
1.  **OS 層面 (Hardware Context)**:
    *   OS 記的是 **M** 的進度。
    *   紀錄 **PC (Program Counter)** 指向哪一行 **機器碼 (Assembly)**。OS 完全看不懂 Go 代碼。
2.  **Go Runtime 層面 (Business Logic Context)**:
    *   Go 記的是 **G** 的進度。
    *   紀錄 `g.sched.pc` 指向哪一行 **Go 源碼**。這一切發生在 **User Space**，OS 完全不知情。

#### 代碼視角：兩層恢復的真相 (The Code Reality)
```assembly
// 1. OS 層面：恢復 M (Thread)
// 當 OS 決定讓 M1 回來執行時，它會執行類似這樣的指令：
MOV RIP, [TCB_M1.RIP]  // 將 CPU 的指令指針 (PC) 恢復到上次 M1 暫停的位置 (例如 0x0045AF)
JMP RIP                // 跳轉執行 -> M1 繼續跑 (就像沒停過一樣)

// 2. Go Runtime 層面：恢復 G (Goroutine)
// 當 M1 醒來後，它發現自己正在執行 schedule()，決定把 G1 換成 G2：
MOV RIP, [G2.sched.pc] // 從 G2 的結構裡讀出上次停在哪一行 (例如 main.go:10)
MOV RSP, [G2.sched.sp] // 恢復 G2 的 Stack 指標
JMP RIP                // 跳轉執行 -> G2 繼續跑
```

**關鍵結論**:
*   **CPU 只聽 OS Thread (M) 的話**：CPU 永遠只執行 M 給它的指令。
*   **M 聽 Runtime 的話**：M 醒來後，會執行 Runtime 代碼 (`schedule`)，由 Runtime 決定把 CPU 的指針 (`RIP`) 指向哪一個 G 的代碼。
*   所以，**最終執行什麼，是由 M 腦中的 Runtime 邏輯決定的**。

### 3. 生殺大權：P 只是容器
*   **P (Processor)**: 只是 **經理的辦公桌 (容器)**，用來放待辦清單 (Queue) 和工具 (mcache)。P 不會思考。
*   **M (Machine)**: 是 **苦力 (執行者)**，負責把 G 從 P 的桌上拿起來跑。
*   **調度器 (Scheduler)**: 這是寫在 M 腦中的 **SOP**。是這套 SOP 決定了「現在跑 G1，等下踢掉換 G2」。

### 4. 核心驅動：schedule() 迴圈
M 的一生其實就在做兩件事：**「跑 G」** 和 **「跑 schedule()」**。
*   **schedule() 的任務**: 幫 M 找到下一個 G。
    *   **優先**: 找自己 P 的 Local Queue。
    *   **其次**: 去 Global Queue 拿。
    *   **最後**: 去偷別人的 (Work Stealing)。
*   **觸發時機**:
    1.  **G 結束** (`goexit`)
    2.  **G 卡住** (`gopark` - 等 Channel/Net)
    3.  **G 被踢** (Preempted)

### 5. 糾察隊長：Sysmon 與搶佔 (Preemption)
如果一個 G 寫了死迴圈不肯出來怎麼辦？
*   **Sysmon (System Monitor)**:
    *   這是一個**不需要 P** 的特殊 M，專門負責巡邏。
    *   它每 **10ms** 檢查一次。
*   **執法**:
    *   如果發現某個 P 跑同一個 G 超過 10ms。
    *   Sysmon 會發出 **信號 (Signal)** 或 **標記**，強制中斷該 M，迫使它呼叫 `schedule()` 把 G 換下來。這就是 **Preemption**。

---

## 7. 第七樂章：硬體的真實代價 (The Hardware Reality)

我們經常談論 M (Thread) 被切換，但這背後到底發生了什麼？為什麼 GMP 要如此費心地設計 Local Queue？
這一切都跟 **OS 的魔法** 與 **硬體的現實** 有關。

### 1. OS 的魔法：無縫接軌 (Registers)
當 M 被 OS 暫停 (Context Switch Out) 去執行其他程式 (如 Chrome) 時，它就像是被按下了 **暫停鍵** 的電影。

*   **Netflix 隱喻**:
    *   **播放中**: 你 (CPU) 正在看電影 (執行 M)，看到 **52 分 30 秒 (PC)**。
    *   **暫停 (Save)**: 媽媽突然關電視。OS 迅速在筆記本 (TCB) 上寫下：「看到 52:30」。
    *   **空白期**: 過了一週，這期間 CPU 去跑了無數個其他程式，腦中關於這部電影的記憶早就被覆蓋了。
    *   **回歸 (Restore)**: 輪到 M 回來時，OS 翻開筆記本，把進度條直接拉回 **52:30**。
*   **結論**: 對 M 來說，時間從未中斷。它感覺前一秒剛抬腳，下一秒就踩下去了。這就是 OS 透過 **保存/還原暫存器 (Registers)** 實現的魔法。

### 2. 硬體的現實：殘酷的腰包 (Cache / The Utility Belt)
然而，OS 的魔法是不完整的。它只還原了你的 **記憶 (Registers)**，卻沒還原你的 **環境 (Cache)**。

*   **腰包隱喻**:
    *   **腰包 (L1 Cache)**: 如同 **Book 1.2** 所述，CPU (館長) 身上綁著一個用來加速工作的腰包。
    *   **現實 (M 醒來的那一刻)**:
        *   當 M 被 OS 喚醒 (從睡眠中回來) 時，腰包裡的資料早就被 Chrome 清空了。
        *   **那一瞬間，絕對是 Cache Miss (冷)**。
    *   **不該忽略**: 所以你不能忽略 Thread 的 Cache Miss。讓 M 睡著再醒來的成本非常高 (100ns起跳)。

    **舉例：哪些行為會導致這種「凍結 (Cold Cache)」？**
    1.  **被搶佔 (High System Load)**:
        *   假設你的 Go 程式跟 **PostgreSQL** 跑在同一台機器上。
        *   OS 覺得 DB 很忙，強制暫停你的 M，切換去跑 DB 的執行緒。
        *   **結果**: L1 Cache 被 DB 的 SQL 查詢資料填滿。你的 M 回來時，腰包全是別人的東西。
    2.  **阻塞式系統呼叫 (Blocking Syscall)**:
        *   M 呼叫了一個耗時的 Syscall (例如讀取超大檔案或 CGO 呼叫)，被迫進入核心態等待 I/O。
        *   OS 趁機切換去執行別的 Process (如 YouTube 音樂)。
        *   **結果**: 等 I/O 好了 M 回來時，腰包已經涼了。

### 3. GMP 的反擊：數據親和性 (Data Affinity)
這就是為什麼 Go 要設計 **GMP 模型**，盡量 **不讓 M 睡著**。

*   **A. 讓 M 保持清醒 (Spinning)**:
    *   Go 寧願讓 M **空轉 (Spinning)** 一下子去偷工作，也不願輕易呼叫 OS 把它掛起 (Park)。
    *   **目的**: 只要 M 還是醒著的 (佔用 CPU)，腰包就不會被別人搶走。
    *   **結果**: 當 M 從 G1 切換到 G2 時 (G-Switch)，因為腰包還在身上，所以能享受 **Cache Hit (熱)**。

*   **B. 數據局部性 (mcache)**:
    *   每個 P 都有獨立的 **`mcache`**。所有在 P 上跑的 G，其變數都分配在這一小塊連續記憶體中。
    *   **結果**: 這大幅提升了 **Cache Hit Rate**。且因為 `mcache` 是 P 私有的，分配記憶體時 **不需要搶鎖 (Lock-Free)**，這也是 Go 效能高的關鍵原因之一。(詳細機制留待 **Book 2.2 Heap** 探討)。

**最終結論：榨乾 CPU 的每一滴價值**

如果你覺得上述的 Cache 知識太過深奧，請記住這核心精神就好：
1.  **其他語言 (Thread per Request)**: 遇到 I/O (如讀檔、網路)，Thread 只能 **「去睡覺」**。OS 把 CPU 切給別人，導致 **Context Switch** 與 **Cache 冷卻**。
2.  **Go 語言 (GMP)**: 遇到 I/O，Thread (M) **「不睡覺」**。它立刻換另一個 G 來跑。
3.  **結果**: **Cache Hit 或許難以精準控制，但「讓 CPU 忙碌」是實實在在的。** Go 確保了 CPU 永遠有事做，而不是在等 I/O 睡覺，這才是最穩定的效能保證。

**一句話總結**: GMP 的設計只有一個目標 —— **把昂貴的 CPU 時間用好用滿，絕不浪費在無意義的等待與切換上。**

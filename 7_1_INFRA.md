# Book 7.1: The Infrastructure (基礎設施：從電纜到 IP)

歡迎來到網路世界的地基。
在您能寫出 `net.Dial` 之前，我們必須先理解這個世界是如何運作的。
這一章探討的是 **L2 (Data Link)** 與 **L3 (Network)**，以及讓這一切運轉的 **DNS**。

---

## 1. The Addressing (一場物流接力賽：為什麼要有兩種地址？)

要搞懂網路這頭巨獸，你必須先建立一個核心認知：**網路傳輸的本質，是一場無止盡的「物流接力賽」**。
在每次發送封包時，其實資料外面包裹著 **兩層信封**：

1.  **內層信封 (IP 地址)**：寫著 `[目的地：美國紐約的 Google (8.8.8.8)]`。
    *   **大戰略**：宣告這個封包「最終」要去哪裡。從台灣出發到紐約抵達，這個內層信封的目標**永遠不會改變**。
2.  **外層信封 (MAC 地址)**：寫著 `[下一站：我家巷口的路由器]`。
    *   **短程交通**：因為你的網卡智商很低，它不知道美國在哪，它只能把電流傳給連在同一條線路上的設備。
    *   **關鍵機制**：這個外層信封，**每經過一台路由器，就會被無情撕掉，然後重寫一個新的外層信封**，指明下一個接力員在哪裡。
真實的接力賽過程如下（以台灣連線到美國 Google 為例）：

*   **📍 第一跳 (您的電腦 -> 家裡路由器)**
    *   **內層包裝**：`[IP 去: Google (8.8.8.8)]` 
    *   **外層包裝**：`[MAC 去: 家裡路由器]`
    *   電腦把封包丟給身邊的路由器。
*   **📍 第二跳 (家裡路由器 -> 中華電信機房)**
    *   路由器收到包裹後，**無情撕掉外層包裝 (MAC)**！
    *   看一眼內層 IP：「喔，這要去美國 Google。」
    *   路由器印出一張全新的外層包裝：`[MAC 去: 中華電信機房]`，把封包從光纖丟向中華電信。
*   **📍 第三跳 (中華電信 -> 太平洋海底電纜...)**
    *   就這樣不斷撕掉舊 MAC、換上新 MAC 指明下一站。但最核心的 IP 永遠指著美國。

這就是 MAC 與 IP 共存的理由：**IP 負責指引大方向，MAC 負責每一站的短程交接**。

現在，我們來看看這兩種信封的物理真面目與名詞定義：
### 1.1 MAC Address (L2 物理地址：短程外包裝)
*   **格式**: `00:1A:2B:3C:4D:5E` (48-bit)。
*   **意義**: **燒錄在網卡上**的全球唯一 ID（相當於設備的硬體指紋）。
*   **局限**: 它**只能在區域網路 (LAN) 內溝通**。一旦出了這台分享器，外面的世界就再也沒有人認識這個 MAC 了。
*   **ARP (大吼大叫找鄰居)**:
    *   **情境**：電腦寫好了 IP 內層信封去 Google，也知道自己必須把信交給家裡的 Gateway (`192.168.1.1`)，但**電腦不知道 Gateway 的 MAC 地址**，所以沒辦法印出外層信封！
    *   **解法 (ARP 廣播)**：電腦只好對著區網所有人大吼：「誰的 IP 是 `192.168.1.1`？聽到請回答 MAC！」。Gateway 聽到了就會回傳自己的 MAC (`AA:BB:CC...`)。有了這個 MAC，電腦才能貼上外層信封，把封包順利丟向 Gateway。

### 1.2 IP Address (L3 邏輯地址：內層包裝 / 最終目的地)
*   **格式**: `203.0.113.1` (IPv4, 32-bit)。
*   **意義**: 這是由網管或 ISP (中華電信) 分發的**路由定位地址**（相當於真實世界裡的戶籍地址）。
*   **CIDR (子網掩碼)**: `192.168.1.0/24` 表示前 24 bits 是「區號」，後 8 bits 是「門牌」。
    *   **Loopback**: `127.0.0.1` 代表「我自己 (Localhost)」。寫給這個地址的封包，作業系統會直接在內部轉發，連實體網卡都不會碰到。
    *   **Private IP (私有地址)**: `10.x.x.x` 或 `192.168.x.x`。這些是「社區內部分機號碼」，只能拿來找局域網內的鄰居，**絕對不能上 Internet (公網)**，否則會被 ISP 直接丟掉。

---

## 2. The Routing (封包的旅程)

讓我們把場景具體化：假設你寫了一個 Go 程式，並且**已經成功建立**了 TCP 連線，準備發送一短句 "Hello" 給遠端的伺服器。

```go
// 假設 TCP Three-Way Handshake 已經完成，我們手上有一個暢通的連線
conn, _ := net.Dial("tcp", "203.0.113.88:8080")
conn.Write([]byte("Hello"))
```

當 `conn.Write` 執行的那一毫秒內，這 5 個 byte 的資料是怎麼跨越千山萬水到達 `203.0.113.88` 的？這就是一場無止盡的接力賽。

### 2.1 The Island Analogy (為什麼要找 Gateway?)
**物理上，你的電腦被困在名叫「區域網路 (LAN)」的孤島裡。**
*   你的實體網路線並沒有直通那台遠在天邊的 `203.0.113.88` 伺服器，它只連到了你家裡的小盒子（路由器 / Gateway，假設內網 IP 是 `192.168.1.1`）。
*   **Gateway (路由器)** 就是這座孤島的 **「唯一對外港口」**。因為你的電腦出不了海，所以任何要去公網的信，都必須先進行第一跳：短程交接給 Gateway。

    > **物理真相：什麼是 Routing (路由)？**
    > 想像你的筆電同時插著「實體網路線」又連著「Wi-Fi」。
    > 當你要寄信時，電腦怎麼知道要把訊號從網路線打出去，還是從 Wi-Fi 天線發射出去？
    > **Routing (路由) 表**就是一套交通規則。作業系統會看這封信的最終目的地，來決定把封包推向 **哪一個硬體出口 (網卡名稱如 eth0 或 wlan0)**。

### 2.2 The ISP (網際網路服務供應商)
*   **角色**: 中華電信、AT&T 就是網路世界的 **「跨海物流公司」**。
*   當 Gateway 把封包送出家門後，實際上是進入了 ISP 鋪設的實體線路 (市網光纖、海底電纜)。ISP 負責將你的封包轉送到全球的骨幹網路 (Backbone)。

### 2.3 Hop-by-Hop: 俄羅斯娃娃大裝箱

為了讓你眼見為憑，我們來設定好這場戲的演員名單跟變數：
*   **你的電腦 (PC)**：MAC=`PC_MAC`, IP=`192.168.1.100`, Port=`54321` (已經握手綁定好的)
*   **家裡路由器 (Gateway)**：
    *   對內 (LAN孔)：MAC=`GW_LAN_MAC`, IP=`192.168.1.1`
    *   對外 (WAN孔)：MAC=`GW_WAN_MAC`, 公網 IP=`59.120.1.1`
*   **中華電信路由器 (ISP)**：MAC=`ISP_MAC`
*   **目標伺服器 (Server)**：IP=`203.0.113.88`, Port=`8080`

準備發送 `Hello`！看看它是怎麼變身的：

#### 第一段：在你的電腦裡 (裝箱出貨)
這事發生在作業系統的 Kernel (核心) 裡：
1.  **主角登場**: `Hello` (5 bytes)。
2.  **L4 裝箱 (封裝 TCP Header)**: 把 "Hello" 裝進小盒子，蓋上專屬的 TCP 門牌號碼。
3.  **L3 裝箱 (封裝 IP Header)**: 把小盒子裝進大箱子，寫上最終的目的地戰略地址。
4.  **L2 裝箱 (封裝 MAC Header)**: 查路由表發現目標在外網，必須交給 Gateway。用 ARP 查到 Gateway 內網的 MAC，裝進短程貨櫃。
5.  **發射**: 網卡把封包射進實體網路線裡。

```text
📦 目前封包長相 (電腦發射，剛飛進網路線的瞬間)
┌────────────────────────────────────────────────────────┐
│ [L2 外層 MAC]   Src: PC_MAC       ➡️  Dst: GW_LAN_MAC  │ 👈 指明交給巷口的路由器
├────────────────────────────────────────────────────────┤
│ [L3 內層 IP]    Src: 192.168.1.100 ➡️  Dst: 203.0.113.88 │ 👈 指明最終要去目標伺服器
├────────────────────────────────────────────────────────┤
│ [L4 核心 Port]  Src: 54321         ➡️  Dst: 8080         │
├────────────────────────────────────────────────────────┤
│ [Data]        "Hello"                                  │
└────────────────────────────────────────────────────────┘
```

#### 第二段：家裡的路由器 (偷天換日 NAT)
Gateway 從重砲收到貨櫃後，開始施展讓你能上網的魔法：
1.  **拆卸外包裝**: Gateway 看到外層寫 `GW_LAN_MAC`，就把最外層撕掉丟垃圾桶，拿出 IP 箱子。
2.  **偷天換日 (SNAT 偽裝)**: Gateway 發現寄件人是不能上網的私有 IP (`192.168.1.100`)。
    *   立刻拿出立可白，把箱子上的寄件人塗掉，**改成 Gateway 自己對外的公網 IP (`59.120.1.1`)**。
    *   在腦海的 Conntrack 表記下一筆帳：「那封竄改過的信是 192.168.1.100 寄的，等下人家回信要記得還他」。
3.  **重新包裝登機 (新的 L2 MAC)**: Gateway 拿出全新外層貨櫃，寫上交給下一站中華電信。

```text
📦 目前封包長相 (Gateway 竄改完畢，從 WAN 孔射出去的瞬間)
┌────────────────────────────────────────────────────────┐
│ [L2 外層 MAC]   Src: GW_WAN_MAC  ➡️  Dst: ISP_MAC      │ 👈 全新的貨櫃，交接給中華電信
├────────────────────────────────────────────────────────┤
│ [L3 內層 IP]    Src: 59.120.1.1  ➡️  Dst: 203.0.113.88 │ 👈 ⚠️ 寄件人被塗改了 (SNAT)！
├────────────────────────────────────────────────────────┤
│ [L4 核心 Port]  Src: 54321       ➡️  Dst: 8080         │
├────────────────────────────────────────────────────────┤
│ [Data]        "Hello"                                  │
└────────────────────────────────────────────────────────┘
```

#### 第三段：ISP 與無止盡的接力 (骨幹網路)
離開家裡進入中華電信的機房後，封包就踏上了網際網路 (Internet) 的無情公路。
1.  在接下來經過的每一台大型路由器、每一條跨國海底電纜中，這套動作會重複狂跑十幾次：
    *   **「撕掉舊的 MAC 貨櫃 ➡️ 看一眼 IP 箱子要去哪 ➡️ 裝進新的 MAC 貨櫃丟給下一站」**。
2.  **抵達終點**: 終於抵達了目標設備 (`203.0.113.88`)。

```text
📦 目前封包長相 (抵達 203.0.113.88 伺服器網卡的前一刻)
┌────────────────────────────────────────────────────────┐
│ [L2 外層 MAC]   Src: 某邊緣路器 MAC ➡️ Dst: 伺服器 MAC    │ 👈 外包裝已經不知道被換過幾次了
├────────────────────────────────────────────────────────┤
│ [L3 內層 IP]    Src: 59.120.1.1   ➡️ Dst: 203.0.113.88 │ 👈 內層地址依然堅挺
├────────────────────────────────────────────────────────┤
│ [L4 核心 Port]  Src: 54321        ➡️ Dst: 8080         │
├────────────────────────────────────────────────────────┤
│ [Data]        "Hello"                                  │
└────────────────────────────────────────────────────────┘
```
**大功告成**: 目標伺服器網卡收到後，作業系統一層層剝開洋蔥，看到 Port 是 `8080`，滿意地把最深處的 "Hello"，塞給正在監聽 8080 的 Go 程式。

### 2.4 Verification (眼見為憑: traceroute)
空口無憑，我們可以用指令來驗證這個 **Hop-by-Hop** 的無盡接力過程。
在 Linux/Mac 執行 `traceroute -n 203.0.113.88` (Windows 請用 `tracert`)：

```bash
$ traceroute -n 203.0.113.88
1  192.168.1.1     (2.4 ms)   <-- 第一段交接：你的家裡路由器 Gateway (執行 NAT 的地點)
2  168.95.102.11   (5.1 ms)   <-- 第二段交接：中華電信的機房路由器 (ISP 第一站)
3  220.128.9.22    (7.2 ms)   <-- 第三段以後：ISP 的骨幹網路 Backbone
... 
10 203.0.113.88    (12.3 ms)  <-- 抵達終點：目標伺服器
```

**解讀**:
*   **第 1 跳 (Local)**: 證明了你的電腦真的如同我們說的，把封包外層 MAC 改社交給了內網的 `192.168.1.1`。
*   **第 2 跳 (Last Mile)**: 證明了 Gateway 在塗改完 IP 後，把封包透過實體光纖丟給了它對接的中華電信 (`168.95.x.x`)。每一行都代表你的封包被重新裝進一次新的 MAC 貨櫃裡！

---

## 3. The NAT (Network Address Translation)

這是雲端時代最重要的發明。前面我們看到了「封包改地址」的魔法，而 NAT 主要就是做兩件事：
1. **SNAT (Source NAT)**: 改「寄件人」，讓你**出得去**。
2. **DNAT (Destination NAT)**: 改「收件人」，讓外面的人**進得來**（這也是負載均衡器 Load Balancer 的真面目）。

### 3.1 SNAT (偽裝寄件人 - 讓你出得去)
如果沒有這項技術，世界上就不會有 Wi-Fi 分享器，你的筆電也上不了網，AWS 的私有 EC2 更是連不到外網。
這就是我們在「2.3 第二段」看到 Gateway 用立可白塗改 IP 的過程，我們用圖解再複習一次：

#### The Flow (被塗改的寄件人)
當你從內網電腦 (`192.168.1.100`) 寄信給 Google (`8.8.8.8`)，封包經過家裡路由器時：

```text
📦 SNAT 發生前 (剛抵達家裡的路由器)
┌────────────────────────────────────────────────────────┐
│ [L3 內層 IP]  Src: 192.168.1.100  ➡️  Dst: 8.8.8.8     │ 👈 寄件人是私有地址，出去會被丟棄！
├────────────────────────────────────────────────────────┤
│ [Data]                   Hello                         │
└────────────────────────────────────────────────────────┘

🔻 路由器施展 SNAT 魔法 (Source Address Translation) 🔻

📦 SNAT 發生後 (被路由器塗改完畢，射向公網的瞬間)
┌────────────────────────────────────────────────────────┐
│ [L3 內層 IP]  Src: 203.0.113.1    ➡️  Dst: 8.8.8.8     │ 👈 寄件人被改成路由器的公網地址了
├────────────────────────────────────────────────────────┤
│ [Data]                   Hello                         │
└────────────────────────────────────────────────────────┘
```
*   **動態記帳 (Conntrack)**: 路由器塗改完後，會在腦海 (nf_conntrack 表) 記下「這筆來源是 203.0.113.1 的對話，其實歸 192.168.1.100 所有」。當 Google 回信時，路由器就會靠這本帳本，把信還原塞回給你的電腦。

### 3.2 DNAT (偽裝收件人 - 讓外面的人進得來)
如果你自己架了一個網站，把公網 IP (`203.0.113.1`) 給了全世界，但是這個網站背後其實是由 3 台內網機器 (Pod) 在分攤流量。
當外面的流量打進這台公網機器 (也就是 Load Balancer / Nginx / K8s Node) 時，神奇的事情發生了：

#### The Flow (被塗改的收件人)
當外面的客戶 (`8.8.8.8`) 想要發出 Request 給你的服務，他只能往你的公網收件人打：

```text
📦 DNAT 發生前 (客戶剛把信寄到你的 Load Balancer 門口)
┌────────────────────────────────────────────────────────┐
│ [L3 內層 IP]  Src: 8.8.8.8       ➡️  Dst: 203.0.113.1  │ 👈 收件人是 LB 本人
├────────────────────────────────────────────────────────┤
│ [Data]                  GET /user                      │
└────────────────────────────────────────────────────────┘

🔻 LB / K8s 施展 DNAT 魔法 (Destination Address Translation) 🔻

📦 DNAT 發生後 (LB 把信秘密轉發給躲在內網的 Pod 處理)
┌────────────────────────────────────────────────────────┐
│ [L3 內層 IP]  Src: 8.8.8.8       ➡️  Dst: 10.0.1.5     │ 👈 收件人被偷偷置換成其中一台背後的 Pod
├────────────────────────────────────────────────────────┤
│ [Data]                  GET /user                      │
└────────────────────────────────────────────────────────┘
```

*   **無縫還原 (Reverse DNAT)**: 當 Pod (`10.0.1.5`) 處理完，回信給 LB 時，LB 會偷偷**把寄件人再改回 `203.0.113.1`**。所以客戶根本不知道剛剛是一台 `10.0.1.5` 在幫他服務。
*   **結論**: 我們常聽到的**「負載均衡 (Local Balancer)」本質上就是作業系統層級的 DNAT 黑魔法**。K8s 能夠把流量分派給 Pod，靠的就是在 Linux Kernel 寫死了一推修改 Dst IP 的 DNAT 規則 (iptables / IPVS)。

### 3.3 Full NAT 架構 (廣義雙向 NAT 與 Nginx 的代價)
前面提到的 Gateway SNAT 與 K8s DNAT，都是在作業系統核心 (Linux Kernel L3) 用立可白**直接塗改封包表頭**，速度極快。

但在真實的業界部署中，為了解決複雜的網路痛點，我們最常使用的 Load Balancer 其實是 **Nginx Reverse Proxy**。在架構黑話中，我們稱 Nginx 實現了 **Full NAT (DNAT 收件人 + SNAT 寄件人 兩者同時替換)**。但請注意，Nginx 是一個應用層 (L7) 軟體，它**不是**真正在底層塗改封包，而是透過「代理」達成了「廣義的」 Full NAT 效果。

#### 為什麼非得雙向修改？ (三角路由危機)
假設我們只做狹義的 DNAT（只改收件人，讓客戶直連 Pod）：
當客戶 (`8.8.8.8`) 寄信給 LB (`203.0.113.1`) 時，LB 把信直接塞給 Pod (`10.0.1.5`)：
*   📥 **Pod 收到的信**：`[寄件人: 8.8.8.8 ➡️ 收件人: 10.0.1.5]` (Pod 心想：原來是 8.8.8.8 找我！)
*   📤 **Pod 回信的悲劇**：Pod 處理完要回信給 `8.8.8.8`。因為 `8.8.8.8` 屬於公網，Pod 查了路由表後，會把信直接發給機房的**實體路由器 (Default Gateway)**，這封回信就直接飛回給客戶，**完全繞過了當初進來的 LB**！
*   💥 **客戶崩潰**：客戶原本是寄信給 `203.0.113.1` (LB)，結果突然收到一封來自 `10.0.1.5`（或機房出口 IP）的回信。客戶根本不認識這個人，會認為遭到駭客攻擊或惡意亂流，直接拒絕接收封包 (RST)。這就叫**三角路由悲劇**。

#### The Flow (Nginx 的 Full NAT 雙刀流)
為了解決回程繞徑的問題，Nginx (LB) 決定在轉發信件時，「同時竄改」寄件人和收件人。因為 Nginx 是 L7 Proxy，它的做法是**「把客戶的 TCP 連線終止並拆開，然後 Nginx 自己作為客戶，發起一條全新的 TCP 連線給 Pod」**：

```text
📦 Full NAT 發生前 (客戶剛把信寄到 Nginx 門口)
┌────────────────────────────────────────────────────────┐
│ [L3 內層 IP]  Src: 8.8.8.8       ➡️  Dst: 203.0.113.1  │ 👈 客戶真實找 Nginx
├────────────────────────────────────────────────────────┤
│ [Data]                  GET /user                      │
└────────────────────────────────────────────────────────┘

🔻 Nginx 終止連線，並發起全新連線 (達成廣義 DNAT + SNAT 效果) 🔻

📦 Full NAT 發生後 (Nginx 發送全新封包給躲在內網的 Pod)
┌────────────────────────────────────────────────────────┐
│ [L3 內層 IP]  Src: 203.0.113.1   ➡️  Dst: 10.0.1.5     │ 👈 收件人換成 Pod, 寄件人因為是新連線，理所當然變成 Nginx!
├────────────────────────────────────────────────────────┤
│ [Data]                  GET /user                      │
└────────────────────────────────────────────────────────┘
```

#### Full NAT 的代價與高併發限制
這種架構雖然完美解決了回程因不認識而拒收的問題（Pod 看到是 Nginx 寄的，回信乖乖還給 Nginx，Nginx 再轉交給客戶）。但這種「廣義 NAT」卻為系統工程師帶來了兩個巨大的代價：

1.  **你寫 Go 程式的代價 (`X-Forwarded-For`)**：Pod 完全被蒙在鼓裡，它收到的信寄件人全部擠成同一個 Nginx (`203.0.113.1`)。這就是為什麼你在寫 Go Web 後端時，`request.RemoteAddr` 永遠看不到真實客人的 IP！為了解決這個副作用，Nginx 必須把 `8.8.8.8` 寫進 HTTP Header `X-Forwarded-For` 裡面讓你去拔。
2.  **效能與高併發的毀滅性打擊**：K8s 或 Gateway 執行狹義 NAT 時只是竄改封包 (L3)。但 Nginx (L7) 是真的建立了**兩條獨立的 TCP 連線** (Client ↔ Nginx, Nginx ↔ Pod)。每個連線都會消耗伺服器的 File Descriptor (fd) 以及核心記憶體。這就是為什麼在超高併發、巨流頻寬的場景 (如 YouTube 影片串流、百萬連線 MMO 手遊) 中，絕對不可能單純用反向代理來做 Load Balancer，Nginx 的網卡和 CPU 會因為處理數百萬條 Proxy TCP 連線瞬間癱瘓。

### 3.4 K8s 的 Service 網路魔法 (kube-proxy 底層揭秘)
如果你在寫微服務，你一定對 Kubernetes (K8s) 的 `Service` 不陌生。你甚至常常在設定檔裡看到 `ClusterIP = 10.96.0.1` 這種神秘的 IP。
K8s 內部成千上萬的微服務交互，靠的就是 **DNAT 與 SNAT 的終極組合技**！這一切的幕後黑手，就是跑在每台實體 Node 機器上的守護神：**`kube-proxy`**。

#### 幻影的 ClusterIP (虛擬 IP)
K8s `Service` 分配的 `ClusterIP` (例如 `10.96.0.1`) 是一個**徹頭徹尾的幻象**！
如果你去檢查任何一台機器的實體或虛擬網卡，你**絕對找不到**有哪張網卡自己綁定了這個 IP。那它到底在哪？它只存在於 Linux 核心的 `iptables`（或 `IPVS`）路由規則裡！

#### 規則與底層攔截 (The Injection)
假設今天「用戶微服務」起了 3 個 Pod，他們的真實私有 IP 分別是 (`10.0.1.1`, `10.0.1.2`, `10.0.1.3`)：
1. **注入陷阱**: `kube-proxy` 得知這件事後，會偷偷在「每一台主機」的 Linux 核心中，寫下一條死板的 **DNAT 規則**。
2. **核心法則**: 只要 Kernel 看到有人想送信給 `10.96.0.1` (虛擬 Service IP)， Kernel 必須在千分之一秒內，把收件欄位隨機塗改成 `10.0.1.1 / .2 / .3` 其中一個！

#### 從 YAML 到 CoreDNS 的命名魔法
身為開發者，這一切的源頭都來自你親手撰寫的那份 YAML 部署檔：
```yaml
apiVersion: v1
kind: Service
metadata:
  name: user-service  # 👈 核心關鍵：這串字會被註冊進 CoreDNS！
spec:
  selector:
    app: user-pod
  ports:
    - port: 80
```
當你執行 `kubectl apply` 的那一刻，K8s 控制台會在背景聯動發動兩大機制：
1. **發配虛擬 IP (通知 kube-proxy)**: 隨機發配一個幻象 `ClusterIP` (例如 `10.96.0.1`)，並通知底下所有機器的 `kube-proxy` 去佈置 DNAT 陷阱。
2. **登記網域 (通知 CoreDNS)**: 拔出 YAML 裡的 `name: user-service`，把 `user-service ➡️ 10.96.0.1` 這個字典對應關係，寫進 K8s 的大腦 **CoreDNS** 裡面。

#### K8s 內微服務互相呼叫的完整全景 (The Flow)
假設你的 `訂單 Pod (10.0.2.55)` 想要打一隻 API 給 `user-service (用戶微服務)` 拿資料：

0. **名稱解析 (CoreDNS)**：
   在真正發出 HTTP 請求前，程式（如 Go 的底層 `net` 庫）會先去問 K8s 內建的 CoreDNS：「請問 `user-service` 在哪裡？」。CoreDNS 此時就會回傳給你一個虛擬的規則 IP：「請把信送往 `10.96.0.1 (ClusterIP)`」。這正是 Service Name 機制的起點。
1. **出發 (Pod 內部)**：
   訂單 Pod 拿到虛擬 IP 後，正式對著它射出 HTTP 請求。
   `📦 [立可白塗改前: Src IP=10.0.2.55 (訂單), Dst IP=10.96.0.1 (虛擬幻象)]`
2. **攔截與落網 (Node Kernel)**：
   這個封包踏出訂單 Pod，剛剛抵達 Node 宿主機的核心網路邊界時，立刻觸發了 `kube-proxy` 早就設下的陷阱 (iptables PREROUTING 鏈)。
3. **無情的 DNAT 塗改**：
   Linux Kernel 執行 DNAT，隨機挑選中了活著的用戶 Pod 第二號機 (`10.0.1.2`)！
   `📦 [立可白塗改後: Src IP=10.0.2.55, Dst IP=10.0.1.2 (真實位置)]`
4. **CNI 外送與 SNAT (如果跨機器)**：
   如果 `10.0.1.2` 這顆 Pod 其實跑在別台遙遠的 Node B (192.168.1.12) 上。那這個被改過地址的封包，還要被丟出目前的宿主機 (Node A 192.168.1.11)。此時 K8s 的網路插件 CNI (如 Flannel / Calico) 會在出海關前踩一腳，執行 **SNAT**！把包裹寄件人偽裝成 Node A 的實體 IP，以便能突破底層 VPC 的限制，順利路由到對面的實體 Node B。

**總結（微服務架構的性能救贖）**：
正因為 K8s 內部這極為龐大、錯綜複雜的微服務互叫，全是依靠在 OS 核心層 (L3/L4) 進行無情的**物理塗改 (DNAT)** 來分發。它完全避開了 Nginx 那種「L7 Proxy 解開封包、中斷連線、代抽新連線」的兩層 TCP 沈重耗損。這正是微服務架構能支撐上百萬次內部 API 呼叫、卻不會癱瘓機器效能的究極物理設計！

### 3.5 致命衝突：當 K8s (L4 塗改) 遇上 gRPC (HTTP/2)
讀懂了前面的機制後，如果你將這份 K8s (L4 Load Balancer) 的知識，結合我們在 `7_3_APPLICATION.md` 學到的 HTTP/2 (多路復用) 知識交叉比對，你會發現一個足以導致災難的**物理級互斥**！

#### 為什麼 gRPC 在 K8s 預設 Service 下會徹底「負載不均衡」？
K8s kube-proxy 的 `iptables/IPVS` 是一種 **L4 (Transport Layer) 的負載均衡**。
所謂 L4 負載均衡，代表它**「只在建立 TCP 連線的那一瞬間，擲骰子決定塗改成誰 (DNAT)」**。一旦 TCP 三次握手成功後，這條 TCP 管線的雙方 IP 就在 Kernel 的 `conntrack` 表裡面死死綁定了。

可怕的地方來了：**gRPC 是跑在 HTTP/2 之上，而 HTTP/2 的精髓是「永遠只用同一條 TCP 連線，不斷在裡面塞入切片 (Stream IDs)」**。

*   **悲劇發生**：當 `訂單 Pod` 啟動時，它對 `user-service (10.96.0.1)` 建立連線。Kernel 在那一瞬間擲骰子，把這條 TCP 水管導向了 `用戶 Pod A`。
*   此後，`訂單 Pod` 送出的第 1 個、第 100 個、第 1 萬個超高併發 gRPC 請求，**全部都會在那同一條永遠不斷開的 TCP 水管裡飆車。**
*   **結果**：`用戶 Pod A` 的 CPU 直接飆到 100% 被活生生打死；而旁邊待命的 `用戶 Pod B` 跟 `用戶 Pod C` 因為分不到任何「新的 TCP 連線」，導致負載完美掛蛋 (0%)。

#### 業界的兩大救贖方案
為了解決這個 TCP 長連接導致 K8s L4 負載均衡失效的問題，架構師通常有兩種解法：

1. **Client-Side LB (Headless Service) 👉 成本最低、最常見**
   * 修改 YAML 將 Service 設為 `ClusterIP: None` (這叫做 Headless Service)。
   * 此時 K8s **不會再建立虛擬 IP，也不會再設置 kube-proxy 規則**。
   * **CoreDNS 的改變**：當 Go 程式查 `user-service` 時，CoreDNS 不再回傳 10.96.x.x，而是直接「一口氣回傳所有背後 Pod 的真實 IP 列表」 (`[10.0.1.1, 10.0.1.2, 10.0.1.3]`) 給你的 Go 程式。
   * Go 內建的 gRPC Client 拿到三個 IP 後，會**在應用程式內部自己打開 3 條 TCP 連線**，並由 Go 程式自己執行 Round-Robin 輪詢發送碎片。把 Load Balancing 的職責從 OS Kernel 拉回給 Application。
2. **Service Mesh (Istio / Envoy) 👉 企業級旗艦解法**
   * 在每個 Pod 裡面強制安插一個 Envoy 代理器 (Sidecar Proxy)。
   * **機制**：Envoy 是一個看看得懂 HTTP/2 的 L7 代理。它會代你收下連線，並且針對「每一個 gRPC 碎片 (Stream ID)」精準分配該射給 Pod A、B 或 C，徹底解決了負載不均。
   * **代價與效能痛點**：因為它本質上就是「每個 Pod 旁邊都掛了一台專屬超小型 Nginx」。這意味著每一次微服務彼此呼叫，**都會多出兩套額外的 TCP 握手、Context Switch 與用戶態/核心態的記憶體複製損耗**。在極限高併發下，這會顯著增加網路延遲 (Latency)。
   * **未來的救贖 (eBPF)**：為了解決這個 L7 Proxy 帶來的效能災難，目前 Kubernetes 業界最前沿的技術 (如 Cilium 或 Istio Ambient Mesh)，正試圖利用 **eBPF** 技術，把 Envoy 這套複雜的 L7 切片解析能力，直接「塞回」Linux Kernel 作業系統核心裡面，打造出能兼顧效能與精準分配的 **無代理 (Sidecarless)** 終極型態！

---

## 番外篇：Domain 是怎麼變成 IP 的 (DNS 運作原理)

前面的篇幅我們一直假設你已經知道對方的 IP。但真實寫程式時，我們往往只有一串網址（或 Kubernetes 裡的微服務名稱）。
當你在 Go 裡寫下：`net.Dial("tcp", "google.com:80")`，它是怎麼變成上面講的那一連串連線的？

```go
// 這是 Go 標準庫 net 裡面的簡化邏輯 (隱藏在幕後幫你查的過程)
func Dial(network, address string) (Conn, error) {
    host, port, _ := SplitHostPort(address) // 拆出 "google.com" 跟 "80"
    
    // 1. 發現 host 是一串文字，不是數字組成的 IP
    if !isIP(host) {
        // 2. 觸發 DNS 查詢 (Go 會偷偷去讀取 Linux 系統的 /etc/resolv.conf)
        // 裡面會寫著 DNS 伺服器的位址 (例如中華電信的 168.95.1.1 或是 K8s 的 CoreDNS 10.96.0.10)
        // Go 接著透過 UDP 協定，發送一封查詢信給 DNS Server：「請問 google.com 的位址是多少？」
        ips, _ := net.LookupHost(host) 
        
        // 3. 拿到真正的真實 IP (例如拿到 "8.8.8.8")
        ip = ips[0] 
    }
    
    // 4. 這時候，系統才具備了真實的收件人 IP，正式呼叫底層往 8.8.8.8 送出包裹！
    syscall.Connect(fd, ip, port) 
}
```

這個機制在 Kubernetes 中被發揮到了極致：
1. Pod 啟動時，K8s 會把 K8s 專屬的 DNS (CoreDNS 的 IP) 塞進你的容器設定檔內。
2. 你對著代碼呼叫 `http.Get("http://payment-service")`。
3. Go 問 CoreDNS：「payment-service 怎麼走？」
4. CoreDNS 回答你一個虛擬的 ClusterIP (`10.96.x.x`)。
5. Go 對著這個 `10.96.x.x` 發送封包，然後在 Node 的 Kernel 門口，馬上就被我們剛剛學過的 **DNAT 魔法** 攔截，替換成了真正活著的某個實體 Pod IP (`10.244.x.x`)。

這就是微服務網路能夠動態彈性「擴縮容」卻不斷線的終極操作！

---

## 4. Summary

*   **網路傳輸的本質**：一場包裹不斷被拆卸再套上新外包裝 (L2 MAC) 的無止盡接力賽。
*   **NAT (立可白魔法)**：
    *   **SNAT**：狹義上是 Kernel 竄改寄件人，解決私有 IP 無法上網的問題；廣義上是指 L7 代理 (如 Nginx) 發起新連線時必定產生的代理效應。
    *   **DNAT**：竄改收件人。它就是 K8s Service IP / iptables 分發流量、達成底層負載均衡的真面目。
    *   **Full NAT 架構**：L7 Reverse Proxy 為了解決「三角路由回程迷路悲劇」的必然後果。代價是消耗巨大的伺服器資源 (雙重 TCP 連線)，且 Go 後端程式必須依賴 `X-Forwarded-For` 才能拿到這場騙局前的真實客戶 IP。
*   **DNS**：把人類友善的名字 (`google.com`) 自動轉換成真實機器的 IP。在 K8s 中，CoreDNS 更是作為動態服務發現的最強大腦。

地基打好後，下一章 **Book 7.2**，我們將進入應用與網路層的橋樑：看 **TCP 與 UDP** 如何踩在這些殘酷的底層泥沼之上，利用 Sequence Number / ACK 機制建立出虛擬的「可靠穩定連線」。

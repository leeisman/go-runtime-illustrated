# Book 7.6: 從連線到崩潰：遊戲微服務的網路全景實戰 (K8s Traffic Flow)

這是一份將前面章節所有網路底層知識（NAT、DNS、TCP、HTTP/2、WebSocket）全部融會貫通的實戰演練。
我們將直接追蹤一個遊戲玩家從「發出操作」到「叢集負載不均崩潰」，再到「架構重構」的完整數據流 (Data Flow)。

---

## 1. 第一樂章：玩家連線的起點 (Client 👉 Ingress)

當百萬玩家同時打開遊戲 App，首當其衝的是維持遊戲即時互動的基礎線路。

1. **連線建立**：玩家手機透過 4G/Wi-Fi，對你的遊戲伺服器網域發起了 `WebSocket` 連線請求（為了追求全雙工的即時主動廣播，見 Book 7.3）。
2. **硬體卸載 (L4 ALB)**：因為高達十萬 QPS 的併發，封包抵達機房的第一線，通常會先撞上雲端供應商的 L4 負載均衡器，在這裡硬體加速把 SSL/TLS 加密外衣給脫掉，將乾淨的封包放入後方。
3. **抵達守門員 (L7 Ingress)**：封包進入 Kubernetes 叢集，打到了 `Nginx Ingress Controller`。這是一個標準的 L7 反向代理，它解析出了網址是 `/ws`，於是終結了外網連線，並在內部發起第二條 TCP 水管，與內網負責扛連線的 `Gateway Pod` 接上線。

**📍 目前狀態**：玩家與你的 `Gateway Pod` 之間，已經維持住了一條永遠不斷開的 WebSocket 長連接。

---

## 2. 第二樂章：內部微服務發車 (Gateway 👉 CoreDNS 👉 kube-proxy)

當玩家在遊戲大廳按下了「查看背包」，Gateway Pod 的 WebSocket 收到了一段 JSON Payload：`{"req": "get_inventory"}`。
為了拿資料，Gateway 必須轉頭向內網的 `Inventory-Service` (背包微服務) 討資料。而在現代高效能架構中，內部通訊必然採用 **gRPC (基於 HTTP/2 多路復用)**。

接下來，就是 K8s 底層魔法連發的瞬間 (見 Book 7.1)：

1. **大腦問路 (CoreDNS)**：
   Gateway 裡的 Go 程式詢問 K8s DNS：「請問 `inventory-service` 在哪？」
   CoreDNS 翻開字典回答：「請將封包寄到這個虛擬幻象地址：`10.96.0.1 (ClusterIP)`」。
   
2. **踩中陷阱 (kube-proxy DNAT)**：
   Gateway 開心地對著 `10.96.0.1` 嘗試建立 TCP 三次握手。封包剛踏出 Gateway Pod，立刻被宿主機 Kernel 的 `iptables` 攔截。
   Kernel 無情地用立可白，將收件人從虛幻的 `10.96.0.1` 塗改成背包服務三號機的真實 IP：`10.0.1.3` (DNAT)。
   
3. **連線確立**：
   這條為了 gRPC 準備的 TCP 連線，最終成功地在 `Gateway Pod` 與 `Inventory Pod 三號機` 之間建起了橋樑。

---

## 3. 第三樂章：致命災難：預設架構下的負載失衡 (L4 遭遇 HTTP/2)

在系統剛上線的第一個小時，悲劇悄悄發生了。營運團隊發現：**Inventory Pod 三號機的 CPU 飆到 100% 冒煙了，但一號機與二號機的負載卻是完美的 0%！**

為什麼 K8s 偉大的負載均衡失效了？這是各種網路特性撞在一起引發的慘案：

1. **K8s 的 L4 盲點**：`kube-proxy` 的 DNAT 立可白塗改法，是一種 **L4 (傳輸層)** 的手段。它**「只在雙方初次建立 TCP 連線的那一瞬間」**會擲骰子隨機分配。一旦 TCP 水管建立，雙方的 IP 就被 Kernel 記入口袋名單 (Conntrack)，所有後續進這根水管的封包，都會被送往固定的三號機。
2. **gRPC 的永不斷線**：偏偏微服務用的 gRPC 跑在 **HTTP/2** 上！HTTP/2 的核心威力叫作「多路復用」——它絕對不會把這條好不容易建立的 TCP 水管關掉。
3. **災難成型**：Gateway 承接了 10,000 個玩家同時「查看背包」的指令。為了追求效能，Gateway 毫不猶豫地把這 10,000 個高併發 Req 剁成極小的 gRPC 碎片，**全部瘋狂塞進「唯一一條」通往三號機的 TCP 水管裡**。

**💥 結果**：K8s 的 L4 負載器覺得自己很無辜，因為它只看到「1 條 TCP 連線」，殊不知這單單 1 條連線裡面，竟然藏了 1 萬筆排山倒海的 L7 API 呼叫，就這樣把三號機活活打死了。

---

## 4. 第四樂章：破局之道：遊戲重構的兩條岔路

為了解平息這個因長連接導致的「餓死不均」問題，系統架構師必須在兩種武器中二選一：

### 路線 A：導入 Service Mesh (懸浮在各處的微型 Nginx)
有些大企業為了解決這個問題，選擇導入 Istio / Envoy。
* **機制**：在叢集裡所有的 Pod 旁邊，強制安插一個 Envoy 代理器 (Sidecar Proxy)。
  * **請求端的錯覺**：你的 Go 程式以為自己直連了目的地，其實它只是把連線掛在了同一台機器的 `localhost` (也就是隔壁的 Envoy) 身上。
  * **無情地全封包解析**：為了把 1 萬筆 gRPC 碎片均勻發給不同的 Pod，Envoy **絕對不能**在建立連線後就不管事休假去。它必須像個過勞的檢查員，日復一日地**攔截經過這條長連接的「每一顆」封包**，吃力地解碼 HTTP/2 表頭、找出是哪個玩家的 Stream ID，再分別射給一號、二號、三號機。
* **致命代價 (遊戲業的禁忌)**：既然是全程攔截的 L7 代理，這意味著你的封包流向變成了：`你的 Go程式 👉 你的 Envoy 👉 網卡 👉 對面網卡 👉 對面 Envoy 👉 對面的 Go程式`。原本一次單純的呼叫，被硬生生扒了好幾層皮，產生多套 TCP 握手與無數次 User-Space 的記憶體大搬風。這看似微小的 2～3 毫秒延遲，在微服務連鎖呼叫的遊戲 Game Loop 裡會雪崩式累積，最終導致遊戲畫面卡頓。這就是極致效能架構堅決不用的主因。

### 路線 B：退回原始，自己做主 (Headless Service) 👉 遊戲後端主流
這是目前絕大數追求效能的遊戲公司與高頻系統唯一信仰的做法：**Client-Side LB (客戶端負載均衡)**。
* **機制 (放棄 K8s 代理)**：我們修改 YAML，把 Service 宣告為 `clusterIP: None`。K8s 明白你的骨氣後，撤銷了所有 kube-proxy 的 iptables 陷阱。
* **CoreDNS 的改變**：此時去問 CoreDNS，它不再給你虛構的 IP，而是大方地把 `[10.0.1.1, 10.0.1.2, 10.0.1.3]` 一口氣全倒給程式。
* **Go 程式的極致運用 (程式碼實作)**：
  在寫 Go 程式時，要觸發這個終極架構非常簡單，只需要傳入兩行關鍵設定：
  1. 使用前綴 `dns:///` 讓 grpc-go 自動觸發內部 Resolver，去問 CoreDNS 拿回一整串實體 IP，而非單一假 IP。
  2. 注入 `round_robin` 輪詢策略，正式把負載均衡的工作從 OS Kernel 拿回 Application 層自己做。
  ```go
  conn, err := grpc.Dial(
      "dns:///inventory-service:50051", // 觸發解析 Headless 陣列
      grpc.WithDefaultServiceConfig(`{"loadBalancingPolicy":"round_robin"}`), // 開啟客戶端自我分配
      grpc.WithInsecure(),
  )
  ```
* **最反直覺的效能秘密 (不需要連線池！)**：
  很多寫習慣 HTTP/1.1 或 MySQL 的工程師，看到這裡會直覺想問：「那我是不是要設定 `SetMaxOpenConns(100)` 掛一個連線池？」**不需要！**
  因為 gRPC 底層是 HTTP/2 的多路復用，對於剛剛拿到手的那 3 台實體主機，`grpc-go` 只會極度克制地**「對每台主機只建立 1 條永遠不斷開的 TCP 連線 (SubConn) 共 3 條」**。
  當 1 萬個背包請求瞬間湧入時，它絕對不會去向作業系統開 1 萬個 Socket，而是會瞬間把這 1 萬個請求切片，然後像機關槍一樣，超高頻率地交錯塞進那僅有的 3 條水管裡。這把作業系統 Socket FD 的負擔壓到了最低，效能反而達到了物理極限！

**🎉 最終勝利**：我們不僅實現了最完美的百萬級負載均衡，還做到了最高級的 **Zero-Proxy 境界**。封包從 Gateway 噴出後，途中不用經過任何 Nginx，不用經過任何 Sidecar，在光纖裡一路狂奔直達目標 Pod，把效能與 CPU 利用率榨到了遊戲工業的物理極限！

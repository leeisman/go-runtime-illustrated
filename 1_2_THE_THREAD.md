# Book 1.2: 真正的館長 —— 執行緒 (The Thread)

在 **Book 1.0** 中，我們介紹了 **館長 (CPU)** 是負責閱讀和執行的主角。但這其實是一個簡化的講法。現在，我們要揭開這座圖書館最核心的運作機制：**裝備與身分 (Identity & Context)**。

## 1. 第一樂章：身分與裝備 (The Gear)

試想一下，**館長 (CPU)** 其實是一個 **裸體的超級勞工**。
他身上有許多 **「裝備插槽 (Registers & PC)」**，但他自己沒有任何記憶或裝備。

當行政區把它指派到 **303 號房 (Process)** 時，面對滿屋子的書本 (Code) 和小白紙 (Memory)，如果沒有裝備，他根本不知道手該往哪擺。

*   **裸體不能工作**：館長必須穿上特定的「數據裝備」，才能變成一個有功能的執行者。

## 2. 第二樂章：裝備庫 (The Thread Pool)

而在 **行政區 (Kernel)** 的 **更衣室 (Kernel Memory)** 裡，有一排排的 **專屬置物櫃 (TCB - Thread Control Block)**。
每一個置物櫃都標有 ID (如置物櫃 A, 置物櫃 B)，用來存放每一位館長脫下來的 **個人裝備**。

在電腦科學裡，這整套機制有一個正式名稱：**Thread (執行緒)**。

*   **Thread (執行緒) = 一整套個人裝備**
    *   這套裝備包含了：
        *   **個人專屬裝備 (Thread Context)**: *這部分在切換時，會被完整存進 **行政區的置物櫃 (Locker/TCB)**。*
            *   **(櫃門標籤)**: 每個置物櫃都貼有標籤（如：**303 號房 - B 號桌**），確保下次能準確回到原本的位置。
            *   **劇本眼鏡 (PC)**: **[容量: 1 個地址 / 8 Bytes]**。一次只能聚焦一行指令。
            *   **數據手套 (Registers)**: **[容量: ~16 個變數]**。一次只能緊握幾個核心數據。
            *   **筆記夾連結 (SP)**: **[容量: 1 個地址 / 8 Bytes]**。一次只能指向小白紙上的一個位置。
        *   **硬體共用裝備 (Hardware Resource)**: *這部分綁定在館長身上，**無法存進置物櫃**，切換時會被倒光。*
            *   **隨身腰包 (L1 Cache)**: **[容量: ~32KB - 64KB]**。
            *   **作用 (Speed)**: 館長要拿資料時，會先摸腰包。如果在腰包裡 (Hit)，只要 **0.5ns**；如果不在 (Miss)，去倉庫拿要 **100ns**。**腰包決定了工作速度。**

*   **預設裝備 (Main Thread)**
    *   當 **303 號房 (Process)** 剛蓋好時，行政區會強制配發 **第一套預設裝備 (Main Thread)**。
    *   所以，每個房間至少都有一套裝備。**館長 (CPU)** 必須穿上這套裝備，才能開始整理這個房間。

### 裝備的代價 (The Cost: 8MB)
這套裝備並不輕便。
當行政區每發出一套裝備 (Thread) 時，他必須同時在 303 號房的角落劃出一塊 **固定大小的保留區**。
如果你申請 3 個 Thread，房間裡就會多出 **3 張專屬桌子**。

*   **這是 Stack 還是 Heap？**
    *   這 8MB 絕對是 **Stack (專屬計算紙)**。
    *   它就像是一盒 **「無限抽取式衛生紙」**，放在那張專屬桌子上。

*   **運作機制 (Infinite Tissue Box)**：
    *   **Call (抽取)**：當館長進入函數 A，就像從盒子裡抽出一張衛生紙鋪在桌上，在上面寫變數。
    *   **Nested Call (疊加)**：呼叫函數 B，再抽一張疊在 A 上面。
    *   **Return (撕掉/覆蓋)**：函數 B 結束，館長直接把最上面那張紙 **撕掉 (Pop)**。
    *   **Overwrite (覆蓋)**：下次再呼叫函數 C，館長再抽一張新紙放在同一個位置，直接 **覆蓋** 掉原本 B 的痕跡。

*   **效率與代價**：
    *   **優點 (No GC)**：這種「用完即撕」的模式非常快，完全不需要垃圾回收車 (Garbage Collector) 來清理。
    *   **缺點 (8MB Limit)**：雖然紙可以一直抽，但桌子上方有 **8MB 的高度限制**。如果你遞迴太深（紙疊太高），就會撞到天花板 (Stack Overflow)。
    *   **浪費**：哪怕你只用了一張紙，行政區還是會為這張桌子保留 8MB 的垂直空間。10,000 個 Thread 就會佔用 80GB，這就是 Thread 昂貴的原因。

## 3. 第三樂章：換裝 (Context Switch)
所謂的多工，其實就是 **館長** 不斷地跑回行政區更衣室。

*   **場景**: 我們先專注在 **303 號房內** 的切換 (Thread Switching)。
*   即便是在同一個房間，館長要從 **桌子 A (負責播放音樂)** 換到 **桌子 B (負責下載歌詞)** 工作，他也必須更換身上的裝備。

### 步驟圖解：
1.  **00:00.000**: **館長** 穿上 **裝備 A**，坐在 **桌子 A** 前，聚精會神地讀著 **第 10 頁**。
2.  **00:00.010**: 行政區吹哨！**館長** 跑回更衣室。
3.  **換裝**:
    *   **脫下 A**：脫下 **劇本眼鏡 (PC)**、**數據手套 (Registers)** 與 **筆記夾連結 (SP)**，全部交給 **行政區 (Kernel)**，鎖進 **行政區專用的置物櫃 A (TCB)**。
    *   **穿上 B**: 從 **行政區置物櫃 B** 拿出 **裝備 B**，翻開到上次讀到的 **第 500 頁**。
4.  **00:00.011**: **館長** 穿著 B 裝備回到 **桌子 B** 繼續工作。

### 代碼視角: OS 如何幫你換裝 (The Code Reality)
我們用底層代碼來破解「換裝」的魔法。假設 Thread A 讀到第 10 行 (`0x10`)，Thread B 上次讀到第 500 行 (`0x500`)。

1.  **切換前 (Running A)**:
    *   CPU 現在的狀態：**`PC = 0x10`**, `RAX = 100`。

2.  **OS 介入換裝 (Context Switch Logic)**:
    ```assembly
    // --- 1. 脫下 A (Save Context) ---
    MOV [TCB_A.RAX], RAX  // 把手套裡的 100 寫入 RAM 裡的置物櫃 A
    MOV [TCB_A.PC],  RIP  // 把在這裡的記憶 0x10 寫入 RAM 裡的置物櫃 A

    // --- 2. 穿上 B (Restore Context) ---
    MOV RAX, [TCB_B.RAX]  // 從置物櫃 B 讀取數據 (假設是 50) 放回手套
    MOV RIP, [TCB_B.PC]   // 從置物櫃 B 讀取進度 (0x500) 放回眼鏡

    // --- 3. 隨身腰包 (L1 Cache) ? ---
    // OS 完全不處理腰包！結果如下：
    // 1. Stack Miss: 腰包裡是 A 的 Stack，B 讀不到自己的 -> 冷 (Miss)。
    // 2. Code Hit:   如果 B 也要執行同樣的代碼，腰包裡剛好有 -> 熱 (Hit)！
    ```

3.  **切換後 (Running B)**:
    *   現在 CPU 的 **`PC` 變成了 `0x500`**。
    *   CPU 就像失憶了一樣，只知道「喔，我現在要執行 `0x500`」，於是它就無縫接軌地繼續 Thread B 的工作了。

這個 **「回更衣室換裝」** 的過程雖然快，但積少成多也是很累人的 (Overhead)。

### 為什麼需要多套裝備？(The Benefit)
為什麼 303 號房不乾脆共用一套裝備就好？

*   **單套裝備的悲劇 (Blocking)**：
    *   **館長** 穿著 **裝備 A** 去下載檔案（需要等待）。
    *   行政區規定：「等待中的裝備必須脫下來掛在牆上，人不能穿著等。」
    *   **結果**：**館長** 脫下 A 後，發現 303 號房 **沒有其他裝備了**。**館長** 只好離開，整個房間徹底停擺。

*   **多套裝備的救贖**：
    *   如果有 **裝備 B**。
    *   **館長** 脫下 A 掛牆上後，立刻穿上 B，繼續在 303 號房裡工作（例如播放音樂）。

## 4. 第四樂章：換裝的隱藏代價 (The Hidden Cost - Cold Cache)

我們常聽說「Context Switch 很慢」，但到底慢在哪裡？
延續上面的例子：**館長從 Thread A (播放音樂) 切換到 Thread B (下載歌詞)**。

### 1. 熱機狀態 (Thread A Running)
*   **代碼視角**: Thread A 正在執行 `x = x + 1` (Stack A)。
*   **腰包狀態**: 裡面裝著 Thread A 最常用的：
    *   **Stack A** 變數 (`0x500`)。
    *   **Shared Code** 指令 (`0x9000`)。

### 2. 切換瞬間 (The Switch)
*   行政區介入，幫館長換了眼鏡和手套 (Registgers)，**但沒有動腰包**。
*   **腰包狀態**: 依然裝滿了 **Stack A (`0x500`)** 的資料。

### 3. 冷機啟動 (Thread B Running)
*   Thread B 接手工作，準備存取自己的變數 (Stack B, `0x800`) 和執行函式 (Code, `0x9000`)。
*   **1. 發現悲劇 (Stack Miss)**：
    *   **現象**: B 伸手去腰包找 `0x800`，但摸到的全是 `0x500` (A 的資料)。
    *   **代價**: 必須去倉庫把 `0x800` 搬回來。這就是切換的主要代價。
*   **2. 不幸中的大幸 (Code Hit)**：
    *   **現象**: B 也要執行 `0x9000` (共用函數)。一摸腰包，居然還在！
    *   **優勢**: 這部分不用去倉庫搬，省下了時間 (Partial Hit)。

### 代碼視角：Cache 的冷與熱 (The Code Reality)
```assembly
// --- 此時腰包內裝著：[0x500] (A Stack) 和 [0x9000] (Code) ---

// 1. Thread B 嘗試讀取自己的變數 (Stack Miss)
MOV RAX, [0x800]   // 請求讀取位址 0x800
// -> CPU 檢查腰包... 只有 0x500
// -> 結果：MISS! (CPU 暫停，花 100ns 去 RAM 搬運)

// 2. Thread B 嘗試執行共用函數 (Code Hit)
CALL 0x9000        // 請求執行位址 0x9000
// -> CPU 檢查腰包... 發現有 0x9000 (A 剛用過)
// -> 結果：HIT! (瞬間執行，只花 0.5ns)
```

**結論**：
*   **Thread 切換**: 會有 **部分 Cache Miss** (主要是 Stack)。
*   **Process 切換 (如換到 Chrome)**: 會有 **全面 Cache Miss** (Stack, Heap, Code 全都不見)。
*   這就是為什麼我們說 Thread 是「輕量級」的 —— 因為它能「蹭」到前一個人的熱度。

## 5. 第五樂章：調度等級 (Scheduling Classes)

這裡要揭露一個現代作業系統 (如 Linux) 的秘密：**行政區調度的單位是 Thread，而不是 Process (房間)**。
這也是為什麼我們稱之為 **Kernel Thread Scheduling**。

行政區的更衣室裡，裝備的擺放位置決定了調度順序：

*   **VIP 金裝 (Real-time / SCHED_FIFO)**：
    *   **機制**：這是 Linux 的 **即時調度 (Real-Time Scheduling)** 類別。
    *   **優先權**：只要架上有這類裝備，館長 **必須優先穿上**。
    *   **搶佔 (Preemption)**：如果館長正穿著銀裝工作，金裝一到，行政區立刻吹哨 (Interrupt)，強制館長換裝。這保證了高優先級任務 (如音訊處理) 永遠不會卡頓。

*   **普通銀裝 (Normal / SCHED_OTHER)**：
    *   **機制**：這是絕大多數程式 (Chrome, VS Code) 使用的類別，由 **CFS (Completely Fair Scheduler)** 管理。
    *   **排隊**：CFS 會確保大家公平地輪流穿裝備 (Time Slicing)，沒有人可以獨佔館長太久。
    *   **Nice 值**：你還可以微調銀裝的「謙讓度 (Nice Value)」，讓它更常被選中或更常禮讓。

## 6. 第六樂章：併發分工 (Concurrency)

最後，讓我們想像一下 **303 號房** 裡火力全開的樣子。
這不是單純的「多人看同一本書」，而是更進階的 **「同房分工 (Intra-process Concurrency)」**。

1.  **初始狀態 (Single Thread)**：
    *   303 號房裡只有一名館長，坐在一張桌子前，讀著一本厚厚的 **「主線劇情書 (Main Flow)」**。

2.  **分身指令 (Thread Spawn)**：
    *   讀到第 10 頁時，書上寫著一行字：**「此處任務過於龐大，請將接下來的三個章節分給不同人同時執行。」**

3.  **拆分章節與增設桌子 (Spawn & Stack Allocation)**：
    *   於是，303 號房立刻在房間角落 **增設了兩張新桌子** (Stack B, Stack C)。
    *   行政區也派發了兩套新裝備 (Thread B, Thread C)。
    *   現在，房間裡有 **三張桌子**，邏輯上我們把書 **拆成了三個獨立篇章**：
        *   **館長 A (裝備 A)**：坐在 **桌子 A**，專攻 **「第一章：網路收發」**。
        *   **館長 B (裝備 B)**：坐在 **桌子 B**，專攻 **「第二章：數據計算」**。
        *   **館長 C (裝備 C)**：坐在 **桌子 C**，專攻 **「第三章：日誌記錄」**。

4.  **結果 (Result)**：
    *   原本一個人要讀 300 頁才能做完的事。
    *   現在三位館長在 **同一個房間** 裡，各自在 **自己的桌子** 上，讀著 **不同的章節**。
    *   **互不干擾，效率倍增**。

這就是現代作業系統強大的地方：**資源共享 (Room)，但任務拆解 (Chapters) 與 獨立作業 (Stack Tables)。**

---

**總結**：
*   **Process (房間)**：資源的容器與書本的所在地。
*   **Thread (裝備組)**：決定了「現在讀到哪」與「草稿寫在哪」。
*   **CPU (館長)**：唯一的肉身，靠著切換裝備來分飾多角，或靠著分身 (多核) 來並行閱讀。
